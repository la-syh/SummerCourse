# AI 使用记录

本文按时间顺序合并项目 Day 1 至 Day 5 的 AI 使用记录。历史内容保持原意；后续与本项目相关的 AI 协作继续追加到本文件。

## 目录

- [Day 1 Task](#day-1-taskai-使用记录)
- [Day 2 Task](#day-2-taskai-使用记录)
- [Day 3 Task](#day-3-taskai-使用记录)
- [Day 4 Task](#day-4-taskai-使用记录)
- [Day 5 Task](#day-5-taskai-使用记录)

## Day 1 Task：AI 使用记录

### 一、使用目的

在完成 Day 1 Task 的过程中，我使用 AI 对代码进行检查、调试和改进。任务内容包括：从 HTML 文档中提取链接，以及提取文档标题和正文并进行中文分词。

AI 主要用于辅助发现代码中的边界情况、解释开发工具给出的类型提示，并验证修改后的程序能否正常运行。程序的最终结构、代码修改和结果确认由我完成。

### 二、具体使用情况

#### 1. 检查初始实现

我向 AI 提供了 `day1.ipynb`，请其检查实现是否满足任务要求。AI 指出了以下问题：

- 初始代码是直接执行的脚本，没有封装成类或函数；
- 同时遍历嵌套的 `div` 和 `p` 可能重复提取正文；
- 使用标签的 `.string` 属性可能遗漏包含子标签的文本；
- 分词结果中可能出现空格和换行等无效词；
- 链接结果中包含 `javascript:;` 之类不能正常访问的值。

#### 2. 改进程序结构

根据检查结果，我将功能封装为 `ExtractHTML` 类，并分别实现：

- `extract_links()`：提取 HTML 中的链接；
- `extract_title_and_body_words()`：提取标题、正文并进行分词。

在正文提取中，我使用 BeautifulSoup 的 `get_text()` 获取标签内部文本，并在使用 jieba 分词后过滤空白词，以减少文本遗漏和无效结果。

#### 3. 解决 Pylance 类型提示

在调用 `href.strip()` 时，Pylance 提示 `href` 可能是 `AttributeValueList`，不能确定其一定具有 `strip()` 方法。AI 解释了这是 BeautifulSoup 类型标注造成的静态检查问题，并建议先判断返回值类型：

```python
href = anchor.get('href')
if not isinstance(href, str):
    continue
```

确认 `href` 是字符串后，再进行清理和过滤：

```python
href = href.strip()
if href and not href.lower().startswith('javascript:'):
    all_links.add(href)
```

采用该方法后，Pylance 类型提示消失，同时程序能够排除空链接和 `javascript:` 链接。

#### 4. 运行环境与结果验证

我说明本任务使用 `conda html` 环境运行。AI 随后在该环境中检查了 BeautifulSoup、html5lib 和 jieba 的使用情况，并执行当前实现进行验证。

验证结果如下：

- 程序可以正常创建 `ExtractHTML` 对象并调用两个方法；
- 共提取到 77 个过滤后的唯一链接；
- 提取结果中不再包含 `javascript:` 链接；
- 文档标题能够正常提取和分词；
- 正文能够正常提取和分词；
- 正文分词结果中没有空白词；
- 原页面没有 `h1`、`h2`、`h3` 标签，因此对应标题列表为空，这属于输入文档本身的情况，并非程序错误。

### 三、AI 建议的采纳情况

我采纳了以下建议：

- 将代码封装为类及类方法；
- 使用 `get_text()` 代替 `.string` 提取文本；
- 使用 `isinstance(href, str)` 进行类型判断；
- 清理链接两端空白字符；
- 过滤空链接和 `javascript:` 链接；
- 过滤分词结果中的空白词；
- 在指定的 `conda html` 环境中重新验证程序。

### 四、使用说明与声明

AI 在本次任务中主要承担代码审查、错误解释和测试建议的作用。我根据任务要求理解并调整了代码，对最终实现和运行结果进行了确认。本文仅记录与 HTML 提取、中文分词、类型检查及运行验证有关的 AI 使用情况。

---

## Day 2 Task：AI 使用记录

### 一、使用目的

在完成 Day 2 爬虫任务的过程中，我使用 AI 辅助理解爬虫的基本结构，检查和调试代码，并逐步实现广度优先抓取、断点续传、URL 过滤、优先级调度、按主机限速以及 HTML 本地保存等功能。

本次协作以讨论、解释和代码审查为主。AI 根据我提出的问题分析现有代码、指出潜在错误，并给出修改建议；我根据任务要求决定是否采纳。例如，AI 曾说明定期重新抓取旧页面的实现方式，但我确认本任务不需要反复抓取，因此最终没有保留该功能。程序的功能范围和最终取舍由我确定。

### 二、具体使用情况

#### 1. 实现爬虫断点续传

我首先询问如何实现断点续传。AI 检查初始代码后，建议保存以下运行状态：

- 待抓取 URL 队列；
- 已经发现的 URL 集合；
- 已处理页面数量；
- 后续加入的优先级及主机访问时间等调度信息。

断点使用 JSON 文件保存。为避免程序在写入过程中退出并留下不完整文件，采用先写临时文件、再使用 `os.replace()` 替换正式文件的方式：

```python
temp_file = self.checkpoint_file + '.tmp'
with open(temp_file, 'w', encoding='utf-8') as file:
    json.dump(state, file, ensure_ascii=False)
os.replace(temp_file, self.checkpoint_file)
```

程序使用 `try`、`except KeyboardInterrupt` 和 `finally`，从而在按下 `Ctrl+C` 时仍然保存当前状态。重新运行后，程序读取断点并继续处理剩余 URL。

#### 2. 解决包导入问题

项目目录为：

```text
hw/
├── day1/
│   ├── __init__.py
│   └── day1.py
└── day2/
    ├── __init__.py
    └── day2.py
```

我在直接执行 `python day2/day2.py` 时遇到 `ModuleNotFoundError`。AI 解释，直接执行文件时 Python 主要从 `day2` 目录寻找模块，可能找不到同级的 `day1` 包。最终采用从 `hw` 目录按模块运行的方式：

```bash
conda activate html
python -m day2.day2
```

同时在相关目录中保留空的 `__init__.py`，明确将其作为 Python 包使用。

#### 3. 检查初始抓取流程

AI 在检查初始代码时指出了几个关键问题：

- `set(url)` 会把 URL 拆成字符集合，应使用 `{url}`；
- `ExtractHTML(url)` 会把 URL 字符串本身当作 HTML，应先通过 `requests` 下载网页，再将返回的 HTML 交给 `ExtractHTML`；
- 使用 `q.popleft()` 才是广度优先队列，使用 `q.pop()` 实际更接近深度优先；
- `__str__()` 必须返回字符串，不能直接返回集合；
- 空的 `except:` 会隐藏超时和连接失败等原因，应捕获 `requests.RequestException` 并输出错误。

在后续版本中，抓取和解析流程调整为：

```python
html = self.get_html(url, headers=self.headers, timeout=(3, 5))
if html is not None:
    links = ExtractHTML(html).extract_links(url)
```

#### 4. 排查程序“卡住”的原因

运行时，终端在一段时间内没有新输出，我询问程序是否卡住。AI 根据代码说明，程序可能正在等待网络请求、请求超时或执行固定的 `sleep()`，而日志只在处理若干页面后打印一次，因此看起来像停止运行。

为方便观察，程序改为每次请求前打印当前进度和 URL，并将超时设置为：

```python
timeout=(3, 5)
```

其中第一个值是连接超时，第二个值是读取超时。AI还建议使用 `flush=True` 及时刷新终端输出，并避免使用空异常捕获，以便区分网络等待和真正的程序错误。

#### 5. 正确处理 URL

为了处理网页中的相对链接、片段和不同协议，AI解释并建议使用：

- `urljoin()`：将相对链接转换为完整 URL；
- `urldefrag()`：删除 `#section` 等页面片段，减少重复抓取；
- `urlparse()`：拆分协议、主机名、路径和查询参数；
- `url_normalize()`：规范化 URL，减少同一页面因写法不同而重复出现。

在 Day 1 的 `ExtractHTML.extract_links()` 中，链接处理流程最终包括：检查 `href` 类型、去除空白、补全相对地址、删除片段、限制为 HTTP/HTTPS，并进行规范化。

#### 6. 过滤压缩包和其他非 HTML 文件

我提出不希望访问 ZIP 等文件。最初只检查 `parsed.path`，但运行时发现下面这种 URL 仍会被访问：

```text
https://www.ruc.edu.cn/cms-proxy/file/download?file=ppt模板.zip
```

AI指出 `.zip` 位于查询参数而不是路径中，因此程序需要同时检查路径和查询参数。最终使用 `urlparse()`、`parse_qs()` 和 `unquote()`，检查可能包含文件名的所有候选字符串：

```python
parsed = urlparse(url)
candidates = [unquote(parsed.path).lower()]

for values in parse_qs(parsed.query).values():
    for value in values:
        candidates.append(unquote(value).lower())
```

随后通过 `endswith()` 检查 ZIP、PDF、Office 文档、图片、音视频等后缀。

此外，AI说明仅根据 URL 不能完全判断资源类型。例如 `/download?id=123` 可能返回任意内容。因此请求使用 `stream=True`，先检查服务器返回的 `Content-Type`；只有 `text/html` 或 `application/xhtml+xml` 才继续读取正文。

#### 7. 实现优先级抓取

任务要求优先抓取“更高质量”的页面。AI建议将普通 `deque` 替换为 `heapq` 优先队列，队列元素采用：

```python
(-priority, sequence, url)
```

因为 `heapq` 是最小堆，所以将优先级取负，使分数更高的页面更早出队。`sequence` 是每次 URL 入队时递增的编号，用于在优先级相同时保持发现顺序。它不能简单由已处理页面数 `count` 代替，因为同一个页面可能一次发现许多新链接，而这些链接需要不同的入队编号。

当前使用的启发式优先级包括：

- HTTPS 页面加分；
- 没有查询参数的 URL 加分；
- 路径越深，分数越低；
- 网站首页加分；
- 与来源页面属于同一主机的链接加分。

AI同时说明，这只是根据 URL 结构估计页面质量，并不等价于真正的内容质量或 PageRank。

#### 8. 实现不同主机的独立访问间隔

为了避免连续快速请求同一网站，程序使用字典记录每个主机最近一次请求完成的时间：

```python
self.last_fetch_time = {}
```

访问前通过 `urlparse(url).hostname` 取得主机名，并只计算该主机的剩余等待时间。访问 `info.ruc.edu.cn` 不会直接沿用 `news.ruc.edu.cn` 的计时记录。请求成功或失败后都会更新时间，以避免持续请求发生故障的主机。

AI也说明了当前单线程实现的限制：如果最高优先级 URL 所属主机仍需等待，整个线程会暂停，而不会立即改抓另一个已就绪主机。考虑到本次作业的规模和实现复杂度，我保留了这一较简单的方案。

#### 9. 保存 HTML 到本地

我明确提出不需要反复抓取旧页面，但需要在成功访问后保存 HTML。AI据此移除了定期重新抓取逻辑，并将 `get_html()` 的返回值保留为 `response.content` 字节。BeautifulSoup 可以直接解析字节，同时保存时不会强制把 GBK、UTF-8 等网页统一转换为另一种编码。

HTML 按主机名分类保存：

```text
downloaded_html/
├── url_index.jsonl
├── info.ruc.edu.cn/
└── news.ruc.edu.cn/
```

文件名使用 URL 的 SHA-256 哈希。AI解释，不能安全地直接用完整 URL 作为文件名，因为 URL 可能包含 `/`、`?`、`#` 等特殊字符，可能超过文件名长度限制，也可能出现同名覆盖或路径穿越问题。

`url_index.jsonl` 用于记录原 URL 和本地文件之间的对应关系。HTML 文件同样采用先写临时文件、再原子替换的方式保存。

#### 10. 配置多个起始网站

我提供了中国人民大学多个学院网站作为起始页面。程序支持传入 URL 列表，并使用：

```python
start_urls = list(dict.fromkeys(start_urls))
```

在保持首次出现顺序的同时删除重复 URL。

调试过程中，AI发现起始 URL 字符串之间缺少逗号时，Python 会自动将相邻字符串拼接成一个无效的超长 URL。补充逗号后，AI通过 AST 检查确认列表中包含 15 个独立起始 URL。

#### 11. 修复断点恢复的数据类型

优先队列在内存中的元素是元组，`all_links` 是集合；但 JSON 读取后，元组和集合都会表现为列表。AI指出，如果直接使用读取结果，会出现以下问题：

- 恢复后的 `frontier` 是列表元素，新入队元素是元组，二者比较时可能报错；
- 恢复后的 `all_links` 是列表，没有 `.add()` 方法，且查找效率降低。

因此恢复时使用：

```python
frontier = [tuple(item) for item in state.get('frontier', [])]
heapq.heapify(frontier)
all_links = set(state.get('all_links', []))
```

同时加入断点文件读取异常处理，避免损坏的 JSON 直接终止程序。

#### 12. Git 版本管理问题排查

在准备推送代码时，我发现 Git 版本状态异常。AI通过只读检查发现：

- 仓库处于 `detached HEAD`；
- 最新提交没有归属到 `main`；
- 工作区存在旧文件删除但尚未提交；
- 新版爬虫仍可能引用已经删除的旧模块路径。

AI建议先建立备份分支，再将当前提交接到 `main`，并在提交删除前确认导入路径是否同步更新。该建议用于避免直接 `git add -A` 后把无法运行的版本推送到远程仓库。

#### 13. 处理正文被误识别为域名的异常链接

运行过程中，`url_normalize()` 抛出了 `idna.core.InvalidCodepoint` 和 `UnicodeError: label too long`。报错中的“域名”实际上包含一大段百分号编码的中文正文。检查原网页后发现，某个异常 `href` 把正常网址和后面的中文说明混在了一起。`urljoin()` 因而生成了一个格式错误的绝对地址，`urlparse()` 又把其中一部分正文放进 `hostname`；随后 `url_normalize()` 尝试按照 IDNA 域名规则处理这段内容，最终报错。

AI说明这不是中文正文或 `url_normalize()` 本身无条件失效，而是输入链接已经不是合法 URL。为避免单个坏链接终止整个爬虫，链接规范化前增加了结构检查，并对规范化异常进行局部处理：

```python
parsed = urlparse(absolute_url)
if parsed.scheme not in {'http', 'https'} or not parsed.hostname:
    continue
if '%' in parsed.hostname:
    print(f'跳过错误域名: {absolute_url}')
    continue

try:
    normalized_url = url_normalize(absolute_url)
except (UnicodeError, ValueError) as error:
    print(f'跳过无法规范化的链接: {absolute_url}, 原因: {error}')
    continue
```

这样处理后，异常链接只会被跳过，其他合法链接仍能继续进入抓取队列。

#### 14. 排查 `clr.ruc.edu.cn` 无法继续向下抓取

抓取化学与生命资源学院网站时，我发现从原入口取得的 HTML 中没有得到预期的后续页面，表现为该主机很快停止扩展。AI协助区分了“请求失败”和“页面成功下载但静态 HTML 不包含可遍历链接”两种情况，并说明部分网站可能依靠 JavaScript 跳转、动态渲染、框架页或特殊入口加载实际内容；仅使用 `requests` 和 BeautifulSoup 的静态爬虫不会执行 JavaScript，因此即使 HTML 已保存，也不一定能发现真正的栏目链接。

AI提出过识别脚本跳转或从页面结构中寻找真实入口的通用方案，但我确认本次任务不需要执行任意 JavaScript 或实现完整的动态网页处理。最初直接将该网站的起始地址改为能够返回静态栏目链接的入口：

```text
https://clr.ruc.edu.cn/zwwz/index.htm
```

修改起始地址后，还检查了断点恢复时新起点的合并顺序：应先把断点中没有的新 `start_url` 加入总优先队列，再按 hostname 拆分到轮转队列。否则新地址虽然可能进入 `all_links`，却不会真正得到调度。最终版本按这一顺序初始化，因此可以保留已有断点并继续运行。

后续对已经保存的页面进行离线统计时，发现共有 412 条 `window.location.href` 或 `location.replace()` 形式的静态跳转，其中 289 个属于允许抓取的起始 hostname、但尚未进入断点。为补回这些页面，最终增加了有限的静态跳转识别：解析 `meta refresh`、`window.location.href='...'` 和 `location.replace('...')` 中直接写出的 URL，但不执行任意 JavaScript。程序升级后的首次启动还会扫描本地已保存 HTML，将漏掉的合法目标合并到现有优先队列；恢复完成后在断点中记录版本，避免以后每次启动都重复扫描。

### 三、AI 建议的采纳与调整情况

我采纳了以下建议：

- 使用模块方式运行 Day 2 程序；
- 使用队列或优先队列维护待抓取 URL；
- 使用集合进行 URL 去重；
- 使用 JSON 保存断点，并采用临时文件原子替换；
- 使用 `urljoin()`、`urldefrag()` 和 `urlparse()` 正确处理 URL；
- 同时检查 URL 路径、查询参数和响应 `Content-Type`；
- 使用 `heapq` 实现启发式优先抓取；
- 使用字典记录不同主机的最近访问时间；
- 保存 HTML 原始字节，并使用哈希文件名和 JSONL 索引；
- 恢复断点时重建元组、集合和堆结构；
- 在 URL 规范化前检查主机名，并跳过无法规范化的异常链接；
- 对不提供可遍历静态链接的 CLR 入口改用明确的静态栏目页；
- 有限识别静态页面跳转，并从已保存 HTML 中一次性恢复漏掉的目标；
- 在 `conda html` 环境中进行语法、导入、队列、断点和 HTML 保存测试；
- 在 Git 推送前检查分支状态、未提交删除和模块导入路径。

我根据任务实际要求调整或未采纳的建议包括：

- 不限制只能抓取 `ruc.edu.cn`，因为任务需要继续访问页面指向的外部网站；
- 不启用定期重新抓取旧页面，只进行一次性抓取和断点续传；
- 不执行任意 JavaScript，也不实现动态渲染页面的通用解析，只识别直接写出目标 URL 的静态跳转；
- 保留较简单的单线程按主机等待机制，没有实现多线程或复杂的主机调度器。

### 四、验证情况

AI在我指定的 `conda html` 环境中进行了不发起公网请求的检查，包括：

- Python 语法和依赖导入检查；
- 多起始 URL 初始化；
- `heapq` 优先队列入队和出队；
- ZIP 文件位于查询参数时的过滤；
- 异常正文进入 hostname 时的 URL 过滤与容错；
- CLR 新入口与旧断点合并后的调度检查；
- 对已保存 HTML 进行静态跳转恢复的内存模拟，确认可补回 289 个目标；
- HTML 原始字节保存；
- URL 与文件索引生成；
- JSON 断点保存；
- 起始 URL 列表元素数量检查。

网络抓取本身具有外部网站状态、网络环境和服务器限流等不确定性，因此正式的大规模运行由我在本地环境中启动并观察。

### 五、使用说明与声明

AI在本次任务中主要承担需求澄清、概念解释、代码审查、错误定位、修改建议和局部验证工作。AI在获得文件写入授权后，也曾协助生成或修改部分代码文件；我根据课程要求决定最终保留的功能、文件结构、抓取范围和运行参数，并负责确认最终提交版本。

本文记录了我与 AI 围绕 Day 2 爬虫任务的主要协作过程。最终代码、运行结果和提交内容由我检查并负责。

---

## Day 3 Task：AI 使用记录

### 一、使用目的

在完成 Day 3 倒排索引作业的过程中，我使用 AI 辅助检查未完成的 `Inverted_index.py`，排查运行错误，并核对 docID 生成、倒排表构建、索引持久化和加载的逻辑。

本次协作以代码审查、错误解释和修改建议为主。AI 未直接修改 `Inverted_index.py`，代码的具体修改由我根据作业要求完成。

### 二、具体使用情况

#### 1. 检查初始的倒排索引实现

我请 AI 检查尚未完成的 `Inverted_index.py` 是否正确。AI 对照 Day 3 课堂 notebook 和已保存的爬虫数据进行了检查，认为倒排表的核心构建思路基本正确：按 docID 顺序处理文档，并通过比较 postings list 的最后一个 docID，可以避免同一个词在同一文档中被重复记录。

AI 同时指出了以下问题：

- 已存在索引文件时直接从构造函数返回，但没有将索引读入 `self.inverted_index`；
- 输出目录不存在时，`open(..., 'w')` 不会自动创建父目录；
- 直接执行 `python day3/Inverted_index.py` 时，可能无法从同级目录导入 `day1`，应当从 `hw` 目录使用模块方式运行；
- HTML 文件路径硬编码为 `downloaded_html/`，因此依赖当前工作目录；
- `make_docID()` 捕获异常后只打印错误，可能使后续代码继续读取不存在或过期的文件；
- 未过滤空白、标点和其他无意义词项，会降低索引质量。

`query_AND()` 和 `query_OR()` 尚未完成，因此本阶段主要检查索引构建与保存逻辑。

#### 2. 排查索引输出目录不存在的错误

使用模块方式运行后，程序在写入索引时报出：

```text
FileNotFoundError: [Errno 2] No such file or directory:
'inverted_index/inverted_index.jsonl'
```

AI 解释，`open()` 可以创建不存在的文件，但不会创建父目录，因此建议在写入前创建输出目录：

```python
output_dir = os.path.dirname(inverted_index_path)
if output_dir:
    os.makedirs(output_dir, exist_ok=True)
```

AI 还说明，终端中的 `Prefix dict has been built successfully.` 是 jieba 初始化成功的提示，不是异常。

#### 3. 检查修改后的保存与加载逻辑

我增加了 `make_inverted_index()`、`save_inverted_index()` 和 `load_inverted_index()`，并加入 `remake` 参数。AI 再次检查后确认：

- 输出目录自动创建的问题已修复；
- docID 已改为从 0 开始，与课堂 notebook 保持一致；
- 已生成的索引文件是一个包含词项和 postings list 的完整 JSON 对象。

但新的保存实现遍历字典时，只将词项 key 写入文件，没有保存对应的 docID 列表。而加载时又使用 `json.load()` 将整个文件当作一个 JSON 对象，两者的文件格式不匹配。AI 建议直接保存完整字典：

```python
with open(inverted_index_path, 'w', encoding='utf-8') as w:
    json.dump(self.inverted_index, w, ensure_ascii=False)
```

加载时，`json.load()` 返回的是普通 `dict`。为了保留查询不存在词项时的默认 postings list，AI 建议重新包装为 `defaultdict`：

```python
with open(inverted_index_path, 'r', encoding='utf-8') as r:
    data = json.load(r)
self.inverted_index = defaultdict(lambda: [-1], data)
```

AI 还指出，主程序固定传入 `remake=True` 会在每次运行时重新构建并覆盖索引。完成调试后应改为 `False`，只在确实需要重建时才显式开启。

#### 4. 检查 Python 环境

按照本项目的环境要求，AI 激活 `conda ML` 环境进行语法和运行检查。`Inverted_index.py` 的语法检查通过，但运行在导入 `BeautifulSoup` 时报出 `ModuleNotFoundError: No module named 'bs4'`。进一步检查显示当前 `ML` 环境没有 `beautifulsoup4` 包。这表明先前成功构建索引时可能使用了另一个 Python 环境，或者当前 `ML` 环境仍需补充依赖。

#### 5. 使用停用词表过滤词项

我已经准备了 `stopwords.txt`，并询问如何在构建倒排索引时使用它。AI 检查后确认，该文件是 UTF-8 编码，共 1396 行，每行一个中文或英文停用词。

AI 建议在构建索引前只读取一次文件，并保存为 `set`，以便快速判断词项是否应被过滤。遍历分词结果时，先使用 `strip()` 清理词项，再跳过空字符串和停用词。如果希望英文停用词不区分大小写，还应对停用词和索引词项统一使用 `casefold()`，并在后续查询时使用相同的归一化规则。

由于已经存在未经停用词过滤的索引文件，实现过滤后需要使用 `remake=True` 重建一次索引；否则程序会直接加载旧索引，新的过滤逻辑不会生效。

#### 6. 理解 `casefold()` 的作用

我进一步询问 `casefold()` 是否就是将大写字母转换为小写字母。AI 说明，对于普通英文可以这样理解，但 `casefold()` 比 `lower()` 更彻底，主要用于不区分大小写的 Unicode 文本比较。例如，`"Apple".casefold()` 得到 `"apple"`；对中文字符则基本没有影响。在倒排索引中使用它时，建立索引和处理查询词必须使用相同的归一化规则。

#### 7. 检查单词查询和逻辑与查询

我编写了 `query()` 和基于有序 postings list 双指针合并的 `query_AND()`，但代码无法通过 Pylance 静态检查。AI 检查后指出，`query()` 使用 `[1:-1]` 会在删除开头哨兵值 `-1` 的同时误删最后一个真实 docID，正确切片应为 `[1:]`。查询词也应与建索引时一样执行 `strip().casefold()`。

`query_AND()` 中的主要问题是混淆了迭代器和迭代器当前取出的 docID：`iter(now_list)` 返回迭代器，而 `next(iter(...))` 只返回第一个整数；二者不能直接使用大小比较。此外，`next(p1, p2)` 的第二个参数是迭代结束时的默认返回值，不是“同时读取两个迭代器”。AI 建议将两个迭代器与两个当前 docID 分别保存，并补充空查询列表、元素类型和返回值的检查。

#### 8. 复查逻辑与并实现逻辑或查询

我修正了 `query_AND()` 中的迭代器使用，并增加了 `query_OR()`，然后请 AI 再次检查。AI 通过语法编译检查确认当前文件没有 Python 语法错误，并确认 `query_AND()` 的双指针交集主体逻辑已基本正确。

AI 同时指出了三个待修正点：第一，`query()` 还没有对查询词执行 `strip().casefold()`，因而可能无法匹配建立索引时已归一化的词项；第二，`query_OR()` 在其中一个 postings list 耗尽后直接捕获 `StopIteration`，但没有将另一个列表的当前 docID 和剩余尾部加入结果，所以并集会丢失文档；第三，逻辑或中的 `if not now_list: break` 不成立，因为前面的查询词没有结果时，后面的查询词仍然可能有结果。AI 建议在逻辑或合并中使用整数下标，主循环结束后分别追加两个有序列表的剩余尾部。

#### 9. 按 Day 3 任务要求进行最终验收

我请 AI 按照“实现倒排索引、支持 AND 和 OR 布尔查询、在 Day 2 抓取的网页上建立索引”三项要求进行最终检查。AI 没有再次触发完整索引重建，而是对已生成的数据和查询方法进行了只读验证和轻量单元测试。

验收结果如下：

- `url_index.jsonl` 和 `docID.jsonl` 都包含 15947 条记录；
- docID 从 0 到 15946 连续编号，且所有 docID 记录指向的本地 HTML 文件都存在；
- 已生成的倒排索引包含 170665 个词项；
- 每个 postings list 都以 `-1` 开头，后续 docID 升序、无重复且不越界；
- 停用词集合与倒排索引词项没有交集，说明停用词过滤已应用到当前索引；
- `query()` 能处理查询词两端空白和英文大小写，并在词项不存在时返回空列表；
- `query_AND()` 和 `query_OR()` 通过了空输入、单词、多词、无结果词项和有序去重结果等测试。

AI 判断三项核心任务要求已经完成。尚存的非核心问题包括：`query_OR()` 的两条类型错误提示仍误写为 `query_AND`；程序仍依赖从 `hw` 目录启动时的相对路径；当前 `conda ML` 环境中缺少 `beautifulsoup4`，因此在该环境中直接导入完整模块仍需先补齐依赖。

#### 18. 强制为任意查询返回 20 条结果

我进一步明确要求，搜索接口不管搜索内容是什么都必须返回恰好 20 条结果，包括空字符串、全空白查询、无命中查询和正常查询，不能因为内容去重、URL 规范化、结果族限制或查询命中文档较少而返回不足 20 条。AI 因此修改了 `ruc_search/service.py` 中统一的 `SearchService.search()`，使 Web UI 和自动评测同时采用新的补足规则。

新的返回流程分为四级：首先选择相关且满足规范 URL、内容指纹和结果族限制的多样化结果；如果不足，再放宽结果族数量限制；如果仍不足，允许相关候选中内容相同但 URL 字符串不同的页面补位；如果查询本身命中的不同 URL 仍少于 20，则从已经建立索引的语料库中追加其他尚未出现的 URL。补足阶段始终不重复同一个 URL 字符串，最终截取前 20 条。

对于空查询，系统跳过倒排索引匹配，直接从已索引语料中选择 20 个不同 URL；Web 路由也不再因查询参数为空而绕过搜索服务，并将缺少 `key` 参数时的 `None` 与空字符串统一处理。由于当前语料库包含 15886 个不同 URL，任意查询都能够补足 20 条。AI 根据我的明确要求直接修改了统一搜索服务，并使用正常查询、少量命中、完全无命中、空字符串、全空白查询和缺少查询参数的请求进行验证；本次协作记录继续写入根目录总披露文件。

#### 19. 说明 `students_evaluation` 的本地评测方法

我询问如何在本地运行 `students_evaluation`。AI 阅读了基础评测客户端、检索接口和评测说明，区分了两种测试方式：直接导入 `students_evaluation/main/search_engine.py` 中的 `evaluate()` 可以在没有评测服务器时检查返回值、URL 唯一性和查询耗时；运行 `students_evaluation/main/client.py` 则会向配置的评测服务器请求查询并提交结果，由服务器计算 MRR@20，因此必须先有课程提供或本地启动的评测服务。

AI 在 `conda html` 环境中从 `students_evaluation/main` 目录直接调用 `evaluate()`，分别测试“高瓴”“毛佳昕”、空查询和完全无命中查询。四种查询均返回恰好 20 个不同的 URL，返回类型检查通过；首次查询包含 jieba 初始化，耗时约 0.19 秒，后续查询耗时明显更低。本轮 AI 只提供运行方法并执行只读的离线接口测试，没有连接评测服务器、获取正式查询或修改搜索代码。

#### 20. 建立无登录步骤的本地验证集评测器

我提供了课程下载目录中的 `students_evaluation/main/val.csv`，其中包含 20 条验证查询，并要求将测试程序独立放入 `tester/`，删除登录环节，直接读取查询；标准相关 URL 由我自行标注。AI 检查后确认该 CSV 只有 `query` 列，没有标准答案，因此无法仅凭该文件计算真实 MRR。AI 曾使用“查询词共同出现”等规则计算代理指标，但明确指出代理相关性定义与课程服务器的标准答案不同，不应替代正式分数。

AI 在作业仓库外建立同级的 `tester/` 目录，以免将课程验证集混入 `hw` 提交。`tester/client.py` 不再访问 `/login` 或 `/mrr`，而是直接读取原始 `val.csv`，调用 `hw/ruc_search/service.py`，检查每条查询是否返回恰好 20 个不同 URL，并生成包含查询、排名、URL、标题和耗时的 `results.csv`。`answers.csv` 提供 20 条查询的空白标注模板；同一查询可以通过多行填写多个相关 URL。答案填写后，程序会在本地计算 MRR@20、Hit@1/3/5/10/20、Recall@20、Precision@20、MAP@20 和 NDCG@20；答案未填满时只统计已标注查询并显示标注数量。

AI 在 `conda html` 环境中完成语法检查和实际运行，20 条查询均返回 20 个不同 URL；本次运行平均耗时约 0.012 秒，最大耗时约 0.19 秒，最大值包含首次 jieba 初始化。AI 还用合成答案验证了 MRR、Hit、Recall 和 Precision 的计算逻辑。随后我获得了课程评测服务器地址并在 debug 模式完成真实评测，服务器返回 `MRR@20 = 0.4576388888888888`；后续效果比较应以这一服务器分数为准，而不是无标准答案时的代理指标。

#### 21. 分析 MRR 的进一步优化方向

我询问如何继续提高当前约 0.458 的 MRR@20。AI 检查了 `tester/answers.csv`、统一搜索服务和 TF-IDF 排序实现，确认标准答案仍未填写，因此服务器总分暂时不能分解到各条查询，也无法可靠判断一次改动具体改善或损害了哪些查询。AI建议先完成本地答案标注，将当前结果保存为基线，再对每次排序修改执行相同的离线评测，避免仅凭少量人工观察或反复提交服务器调参。

AI 根据现有 20 条查询及结果指出，当前主要排序问题不是返回数量，而是候选顺序：检索器使用带整篇文档向量长度归一化的 TF-IDF，查询时没有短语、邻近度和页面类型信号；列表页可能因为重复出现多个查询词而排在具体文章前面，长查询中的“年”“学院”“大学”等常见词也会共同影响得分。已有结果中，部分语义明显对应的详情页位于第 5、第 8 或第 18 名，另有目标特征明显的详情页未进入前 20，说明前 500 个候选后的二次重排具有较高的改进空间。

AI建议的实施优先级是：第一，在现有前 500 个候选上使用已保存的标题和摘要进行轻量重排，对完整查询短语、标题中的稀有查询词覆盖率和摘要覆盖率加分，并对标题为“学院新闻”“通知公告”“科研”等通用列表页降权；第二，过滤或降低文档频率过高的低信息查询词，尤其改善日期和长句查询；第三，再考虑将底层余弦 TF-IDF 改成带字段权重和词频饱和的 BM25F，并在索引中分别保存标题、页面标题和正文长度；第四，如仍有需要，再增加词语位置以支持短语和邻近度打分。AI 还提醒课程 MRR 可能按精确 URL 判断，当前内容指纹去重有可能保留内容相同的另一个 URL 而删掉标准答案 URL，因此填好答案后还应专门统计“相关 URL 曾进入候选集、但被去重移除”的情况。此轮 AI 只进行了诊断和提出优化顺序，没有在缺少标准答案的情况下直接修改生产排序代码。

#### 22. 使用部分人工标注改进召回与重排

我在 `tester/answers.csv` 中填写了部分标准 URL，并指出当前搜索效果较差，最后一条毕业典礼发言结果仅排第 18。AI 读取文件时发现，表格软件保存后查询列的部分中文 UTF-8 字节已经被下划线替换，无法从该文件恢复原查询，但 URL 和 20 行顺序仍完整。AI 修改本地测试器，使其在查询文本损坏时按照原始 `val.csv` 的相同行号关联标准 URL，且不覆盖我的答案文件。12 条已标注查询的初始本地结果为 `MRR@20 = 0.379630`、`Hit@20 = 0.666667`。

AI 进一步区分了未命中原因：3 个标准 URL 分别位于 `see.ruc.edu.cn`、`phi.ruc.edu.cn` 和 `cjjc.ruc.edu.cn`，没有出现在当前 15886 个 URL 的本地语料库中，单纯调整排序不可能命中；国学院的 2014 年团体赛页面已经下载，但其正文完全使用 `div` 排版、没有 `p` 标签，原解析器因此没有将查询内容写入索引；毕业典礼发言页面存在于前 500 个候选中，但原始排名较低。

AI 在 `SearchService` 中加入候选重排：保留原始 TF-IDF 排名作为基础分，并利用查询词的 IDF 对标题和摘要覆盖率加权，使标题中覆盖更多稀有查询词的详情页优先。重排后，“2019年犀牛鸟基金”和“机器人进化”升至第 1，毕业典礼发言从第 18 升至第 2；已标注集合的 MRR@20 一度提高到 `0.527778`。

随后 AI 修改 HTML 提取和页面元数据生成逻辑：当页面没有任何段落标签时，删除脚本、样式等不可见节点，再退回提取整个页面的可见文字。AI 在 `conda html` 环境中重新解析 15886 个本地页面、重建倒排索引及页面元数据，全部页面元数据生成成功。国学院目标页由无法召回变为原始 TF-IDF 第 3，但由于标题通用、摘要开头为导航文字，被元数据重排压到第 17。AI 因此加入保守护栏，使原始前三名最多下降两位；最终该页位于第 5，毕业典礼发言位于第 3。

最终 12 条已标注查询的本地结果为 `MRR@20 = 0.544444`、`Hit@1 = 0.416667`、`Hit@3 = 0.666667`、`Hit@5 = 0.750000`、`Hit@20 = 0.750000` 和 `NDCG@20 = 0.595726`。20 条查询仍全部返回恰好 20 个不同 URL，平均检索耗时约 0.013 秒；Python 语法检查和 Git 差异空白检查均通过。剩余主要瓶颈是三个标准 URL 不在语料库，需要补充相应站点的抓取覆盖，而不是继续只调排序权重。

#### 23. 使用全文覆盖、文章元数据继续优化排序

我重新运行课程 debug 服务器后，真实结果提高到 `MRR@20 = 0.595378787878788`，平均查询时延为 0.014 秒。我询问是否能够由 `client.py` 返回的结果倒推出测试集答案，并说明自己是课程助教、允许直接运行 debug 客户端，同时要求尽可能优化搜索结果。AI说明单次客户端只返回所有查询的平均 MRR 和平均时延，数学上不能唯一确定每条查询的标准 URL，因为多种不同的逐题排名组合可以产生相同平均值；若要利用服务器反馈定位变化，只能进行受控消融并比较总分，而不能从一次结果直接恢复答案。

AI 在现有标题和摘要重排基础上增加了全文查询词覆盖率：对进入前 500 的候选，利用倒排表二分查找每个候选是否包含查询词，并用查询词 IDF 加权覆盖比例。这样无需读取 HTML 正文，也不需要重建索引，时延增加很小。12 条部分人工标注上的 MRR@20 从 `0.544444` 提高到 `0.641667`，其中毕业典礼发言和“脱口秀梗王”均升至第 1，平均时延约为 0.014 秒。

AI 随后发现国学院旧网页虽然页面 `<title>` 通用，但提供了 `ArticleTitle` 和 `PubDate` 元数据，因此扩展离线元数据生成器以优先读取文章标题，并保存发布日期用于日期型查询重排。重新生成 15886 条页面元数据后，部分标注集的 MRR@20 先提高到 `0.683333`，加入发布日期后进一步提高到 `0.694444`，`Hit@1 = 0.666667`、`Hit@3 = 0.750000`、`Hit@20 = 0.750000`、`NDCG@20 = 0.708333`，平均时延约 0.0147 秒。

AI 尝试直接运行 `hw/students_evaluation/main/client.py` 获取最新服务器分数。第一次在沙箱内因无法访问本机 7897 代理失败；经我授权后，绕过代理直连课程内网地址超时，通过本机代理则返回 HTTP 502，说明当前 Codex 运行环境无法到达该课程内网服务。因此最新真实 MRR 仍需由我在能够访问服务器的本机网络中再次运行。AI 最后完成了 Python 语法检查以及 Flask 正常查询、空查询和缺少查询参数三种请求的回归测试，所有请求均返回状态码 200 和恰好 20 个结果卡片。

我在本机网络中重新运行 debug 评测，服务器返回 `MRR@20 = 0.7683333333333333`，平均查询时延为 0.015 秒。与上一轮真实服务器结果 `0.595378787878788` 相比，MRR 绝对提高约 0.173，而平均时延只增加约 0.001 秒；与最初的 `0.4576388888888888` 相比，累计绝对提高约 0.311。这说明全文查询词覆盖、`ArticleTitle` 文章标题和 `PubDate` 发布日期等通用排序信号在服务器隐藏标准答案上也产生了明显收益，并非只改善本地部分人工标注。

#### 24. 规划 RAG 阶段的实现顺序

我准备开始课程的 RAG 进阶任务，并询问下一步工作。AI 阅读了 `students_evaluation/rag` 中的检索接口、信息整合函数、模型调用封装、评测客户端和 5 条验证问题，确认 `search()`、`snippet_merge()`、`full_merge()` 和 `custom_integrator()` 仍为空实现。验证题涉及多个年份人数比较、个人主页课程、企业参访站次以及两位教师课程交集，单纯使用当前最多 200 字且可能为通用站点文字的摘要不足以稳定回答，需要读取本地 HTML 正文并进行查询相关分块。

AI建议按最小可用链路逐步实现：先让 `search()` 调用现有 `SearchService`，将 URL、标题、摘要及本地 HTML 文件路径关联起来；再实现正文清洗，移除脚本、样式和导航噪声；随后将正文按段落或固定长度分块，依据查询词覆盖率选取相关片段，在总字符预算内合并多个来源；最后再调用模型，并用 5 条 `rag_val.csv` 查询逐题检查事实完整性、答案格式和 60 秒时限。由于这些问题常需多个页面共同回答，默认 `top_k=5` 可能偏小，建议从 10 条候选开始并通过分块控制上下文长度。

AI 在检查配置时发现模型 API Key 以明文写在本地 Python 文件中。AI 没有在回复或披露记录中复述该密钥，并建议立即轮换现有 Key，改为从环境变量读取，防止代码分享、日志或版本控制造成泄露。本轮仅进行了代码与验证题分析并给出实施顺序，尚未修改 RAG 代码或调用外部模型。

#### 25. 说明终端环境变量的安全设置方法

我询问如何在终端设置模型 API Key 环境变量。AI 针对当前 macOS zsh 环境建议使用 `read -s` 隐藏输入，再执行 `export`，使密钥不会显示在屏幕或进入 shell 命令历史；该变量只在当前终端会话及其启动的子进程中有效，关闭终端后自动失效。AI 还说明可通过 Python 仅读取变量是否存在来验证配置，而不打印密钥内容，并建议不要把真实密钥直接写入 `.zshrc`、代码、披露文件或提交到 Git。

#### 26. 使用 macOS 钥匙串永久保存模型密钥

我继续询问能否永久设置环境变量。AI 核对了本机 `security` 命令的帮助信息，建议不要直接把 API Key 明文写入 `~/.zshrc`，而是使用 macOS 钥匙串的 generic password 项保存密钥，并将 `-w` 放在添加命令末尾以交互式隐藏输入。随后可在 `~/.zshrc` 中只保存一条从钥匙串读取密钥并导出 `DEEPSEEK_API_KEY` 的命令，使每个新 zsh 会话自动获得环境变量，而 `.zshrc` 本身不包含真实密钥。AI 还提供了不显示密钥内容的验证方法。本轮只说明配置方法，没有读取、记录或修改真实密钥。

#### 27. 排查永久环境变量未生效

我按说明向钥匙串添加密钥后，直接执行了 `source ~/.zshrc`，但在 `conda ML` 中检查仍显示环境变量未设置；复制的命令还混入了 Markdown 加粗符号和反斜杠，导致一次 `python` 命令无法识别。AI 只读检查了 `~/.zshrc` 中与 DeepSeek 相关的配置行，确认文件中没有自动从钥匙串读取并导出 `DEEPSEEK_API_KEY` 的命令，因此问题不在 Python，而是遗漏了写入 shell 配置这一步。AI 没有读取或显示钥匙串密码，并提醒本项目应激活 `conda html` 环境，同时提供不含 Markdown 格式字符的修正命令。

#### 28. 检查 RAG 是否已经具备运行条件

我完成环境变量配置后询问能否运行 RAG。AI 检查确认 `~/.zshrc` 已包含从 macOS 钥匙串读取并导出 `DEEPSEEK_API_KEY` 的命令，`call_model.py` 也已经改为通过 `os.environ` 读取密钥，RAG 评测客户端的服务器地址已经填写。Codex 沙箱本身无法访问用户钥匙串，因此其子进程显示变量未设置，但这不代表我自己的普通终端配置失败，应以我在普通终端中的检查结果为准。

AI 进一步检查了当前 RAG 实现，确认 `search()` 已接入统一搜索服务、能够返回标题、摘要和本地正文，`full_merge()` 已实现清洗、内容去重、多页面字符预算和正文回退，`rag_evaluate()` 默认使用 `full` 策略。AI 使用虚拟密钥执行了不联网的本地冒烟测试：检索返回 20 条结果，20 条均有正文，合并上下文长度为 11998 字符且没有超过 12000 字符预算，本地检索和正文整合链路通过。

AI 同时发现一个待修正问题：`search(query, top_k)` 内部固定调用 `k=20`，忽略了传入的 `top_k`，所以 `rag_evaluate()` 虽默认写 `top_k=5`，实际上仍会把字符预算平均分给 20 个页面，可能使每页关键正文被过早截断。AI 建议在正式评测前将固定值改为 `k=top_k`，再先对单条验证问题调用 `rag_evaluate()`，确认模型配置和答案质量后运行完整 RAG 客户端。本轮 AI 只进行了只读检查和不联网测试，没有调用外部模型或修改 RAG 实现。

#### 29. 诊断首道 RAG 问题返回“材料不足”

我成功调用模型测试许洪腾老师课程问题，但模型返回“材料不足”。AI 检查前 20 个检索结果和最终 11998 字符上下文，发现上下文包含 2022、2023、2024 和“春季”等一般词，却完全不包含“许洪腾”；前排结果均为其他教师主页或课程通知。因此模型严格依据材料回答“材料不足”是合理行为，问题发生在检索阶段，而不是 API、模型调用或最终提示词。

AI 确认许洪腾个人主页已经存在于本地语料库。使用完整自然语言问题检索时主页未进入前 20，但将查询缩短为“许洪腾”或“许洪腾 教授课程”后，该主页稳定排第 1。AI 从本地正文中核对到“教授课程”栏目包含“机器学习基础：2022-2024春”，因此该验证题答案是“机器学习基础”。这说明 RAG 需要在检索前提取核心实体、栏目名、年份和机构等关键词，不能总把完整多条件问题直接交给关键词搜索。

AI 建议下一步增加查询改写与多跳检索：先生成若干短查询分别检索教师主页、年份活动或公司参访页面，合并并去重结果，再进行查询相关分块和最终回答。对于当前五道验证题，涉及多个年份、多个公司或两位教师的题目尤其需要分别检索多个实体；其中“北京市科技新星计划”问题还需要先从报道识别教师，再检索该教师个人主页。AI 再次指出 `search()` 仍固定返回 20 条、没有使用 `top_k` 参数，这会进一步稀释每页上下文预算。本轮 AI 只完成诊断和事实核对，没有修改 RAG 代码。

#### 30. 实现查询改写、多查询合并和二跳 RAG

我要求 AI 直接修改 RAG 实现。AI 首先将 `search()` 中固定的 `k=20` 改为使用传入的 `top_k`，然后新增查询词提取、查询改写和多查询合并。查询改写会识别教师姓名、引号中的栏目、四位年份、夏令营主题、公司名称和科技新星计划等信息；对于年份比较题分别生成每年查询，对于企业参访题分别生成每家公司查询，对于两位教师课程交集题分别检索两人的教师主页。所有查询结果按 URL 合并去重，并限制总候选数量。

AI 新增了查询相关正文整合策略：使用当前倒排索引的 IDF 选择高信息量查询词，将每个页面正文切成带重叠的固定长度片段，按查询词覆盖加权评分，优先保留相关片段，再在 12000 字符总预算内合并最多 10 个页面。`rag_evaluate()` 默认策略由 `full` 改为 `custom`，避免仅按页面开头截断导致正文中的课程、年份或站次信息丢失。

针对“报道中的入选教师及其个人主页课程”这类问题，AI 还实现了二跳检索：先从包含目标年份的入选报道标题中抽取教师姓名，再追加“姓名 + 教授课程”查询，并将二跳教师主页提前放入上下文候选。公司抽取规则在测试中曾将“华为公司和快手公司”错误合并，AI 随后缩短公司名称匹配范围并去除连接词，使腾讯、华为和快手分别检索。

AI 在 `conda html` 环境中对 5 条 `rag_val.csv` 验证问题执行了不联网回归。五题均获得非空检索结果和不超过 12000 字符的上下文；许洪腾问题包含“许洪腾”“机器学习基础”和“2022-2024春”的完整证据；企业参访问题召回腾讯第 15/16 站、华为第 4 站和快手第 7/14 站；科技新星问题召回林衍凯 2024 年入选报道并加入林衍凯教师主页；课程交集问题同时加入毛佳昕和苏冰主页。Python 语法检查和差异空白检查通过。本轮没有调用付费模型或评测服务器。

#### 31. 根据首轮 RAG 裁判反馈修复空答案和跨页面推理

我运行 RAG debug 评测后，第 1、2 题均获 1 分；第 3、4 题因答案为空得 0 分，第 5 题因回答“材料不足”得 0 分。AI 将问题分为两类：第 3、4 题是客户端捕获模型调用异常后提交空字符串；第 5 题模型调用成功，但没有完成两位教师课程的跨页面交集。

AI 对第 3 至第 5 题的实际上下文进行了逐项核对。第 3 题材料包含腾讯第 15/16 站、华为第 4 站和快手第 7/14 站；第 4 题材料包含林衍凯主页的本科生课程《知识表示与学习》；第 5 题材料显示毛佳昕的《人工智能综合设计》安排在大一夏季学期，苏冰主页将同名课程列为本科生科研与实践环节。因此检索和正文材料已经足以作答，主要剩余问题是调用可靠性和模型指令。

AI 修改 `call_model.py`，对连接错误、超时、服务端错误和限流增加一次短延迟重试；关闭 SDK 内部不受控重试，将单次请求超时限制在最多 25 秒，使两次尝试连同等待仍可控制在课程规定的 60 秒内；同时将最大回答长度从 8192 缩短到 2048，以减少无必要的生成时延。非瞬时配置错误仍会直接抛出，不会被盲目重试。

AI 还强化了最终提示词，明确要求分别提取对象对应的序号后排序、分别读取多个对象材料后求交集、关联报道人物与个人主页，并强调证据可能分散在不同资料中。对于同一公司多次参访而问题未指定“最新”的情况，要求采用最早、站次最小的记录；无论证据是否充分都必须返回非空字符串。AI 检查本地页面标题后确认腾讯存在第 15 和 16 站、华为为第 4 站、快手存在第 7 和 14 站，因此该规则能够消除重复参访造成的歧义。修改后的 Python 文件成功编译和导入，本轮没有再次调用外部模型或评测服务器。

#### 32. 修正企业参访排序题的输出对象

我再次运行 RAG debug 评测，5 题耗时分别约 1.78、1.21、0.83、1.27 和 1.16 秒，平均时延 1.248 秒，总分提高到 0.8。第 1、2、4、5 题均获 1 分；第 3 题仍为 0 分。裁判指出第 3 题答案只给出了站次编号，没有按照站次从小到大输出公司名称，属于输出对象错误，而不是检索事实错误。

AI 根据题意确认应使用站次完成排序，但最终回答的主体必须是公司名称，正确顺序为“华为公司、快手公司、腾讯公司”，对应最早参访记录的第 4、第 7 和第 15 站。AI 修改最终提示词，明确要求当问题按某个数值对多个对象排序时，应输出排序后的对象名称，数值只能作为括号补充；并专门以公司站次排序说明不得只输出数字。本轮只修改答案格式指令，没有改变已经通过验证的检索、正文整合和模型重试逻辑。

#### 33. 撤回针对单题的过度提示词约束

我指出，仅根据企业参访这一道验证题就在通用提示词中加入公司名称和站次的专门示例过于武断。AI 接受这一批评并重新检查上一轮修改，确认“同一公司多次参访时默认选择最早站次”同样是缺少题目依据的假设，容易对其他需要最新记录或特定活动批次的问题造成错误。

AI 删除了公司专用示例和默认选择最早记录的规则，改为通用的答案模式识别原则：先判断问题要求的答案类型与输出对象；排序问题区分被排序对象和排序依据，输出题目明确要求的对象，只有明确询问数值时才以数值为答案主体。对于同一对象的多条记录，要求优先依据年份、活动名称、系列及其他题目限定条件消歧；无法唯一确定时简洁说明歧义，不擅自假设最早或最新。本轮修正只涉及通用提示词，没有硬编码任何验证题答案。

#### 34. 使用结论不变性处理多条候选记录

我再次运行 RAG debug 评测，总分仍为 0.8，第 1、2、4、5 题各得 1 分；第 3 题没有发生模型调用异常，但仍未给出公司顺序，被裁判视为空答案。AI 重新分析材料发现，同一公司存在多次参访记录：腾讯对应第 15 和第 16 站，快手对应第 7 和第 14 站，华为对应第 4 站。删除“默认最早”规则后，模型因无法唯一选择单条记录而过度保守。

AI 没有恢复针对参访题的硬编码规则，而是提出并实现通用的结论不变性原则：同一对象有多个候选值时，先用题目中的年份、活动系列等条件消歧；若仍有多个候选，则判断不同选择是否会改变题目所求结论。如果所有候选组合都导向同一个结论，应直接回答该稳定结论；只有候选选择确实改变结论时才说明歧义。当前材料中，华为的站次始终小于快手，快手的所有候选站次又始终小于腾讯，因此公司相对顺序不受重复参访记录影响。该规则适用于排序、比较、最大最小值和其他多证据问题，不假设最早或最新，也未写入具体公司名称或答案。

#### 35. 将 Kimi API Key 永久保存为环境变量

我要求将 Kimi API Key 加入环境变量。AI 建议沿用此前已经验证的 macOS 钥匙串方案，使用独立变量名 `KIMI_API_KEY`：通过 `security add-generic-password` 交互式隐藏输入密钥，在 `~/.zshrc` 中只保存从钥匙串读取并导出变量的命令，再重新加载 zsh 配置。AI 同时提供了不显示密钥内容的 Python 验证方法，并提醒不要在终端命令、代码、披露文件或 Git 中写入真实 Kimi Key。本轮只提供配置方法，没有读取或修改实际密钥。

#### 36. 排查 Kimi 模型的认证与请求参数错误

我将 RAG 调用切换到 Kimi K3 后，首先遇到了 `AuthenticationError`。AI 检查发现当时配置已使用 Moonshot 的接口地址和 Kimi 模型名，但密钥变量仍与 DeepSeek 配置混用。配置改为读取 `KIMI_API_KEY` 后，认证错误消失，证明钥配置已经生效。AI 没有读取、输出或记录密钥内容。

随后的评测出现三次 `BadRequestError` 和两次 `RateLimitError`。原客户端只显示异常类型，隐藏了服务端返回的具体原因。AI 查阅 Kimi 开放平台当前官方文档：平台首页已宣布 K3，但 Chat Completions 接口和模型列表文档当前明确给出的通用模型 ID 为 `kimi-k2.6`，而账号实际可调用的 ID 应以 `/v1/models` 返回值为准。为先恢复可稳定评测的配置，AI 将默认模型改为 `kimi-k2.6`，同时允许用 `KIMI_MODEL` 和 `KIMI_BASE_URL` 环境变量覆盖，便于以后使用账号列表中的 K3 精确 ID。

AI 还将已弃用的 `max_tokens` 请求参数替换为 `max_completion_tokens`，并去掉了非必需的 `thinking` 扩展参数，减少模型参数不兼容导致 HTTP 400 的可能。评测客户端现在会在异常类型后显示 SDK 提供的服务端错误消息，以便区分模型不可用、参数错误、余额不足和请求频率限制。AI 在 `conda ML` 环境中完成了相关 Python 文件的语法编译；该环境未安装 `openai` 包，因此没有在 Codex 环境内发起付费模型请求或进行真实密钥验证。

#### 37. 讨论课程报告的差异化展示内容

我计划参加课程报告环节，并询问还可以增加哪些独特内容。AI 结合当前项目已经实现的本地网页检索、元数据预提取、去重与多样性、查询词高亮、排序优化和多跳 RAG，建议报告不要只罗列功能，而是围绕“一个本地搜索引擎如何逐步变成可解释、可评测、可追溯的 RAG 系统”组织内容。

AI 提出的可选亮点包括：在搜索结果中展示标题、正文、日期和原始 TF-IDF 对最终排名的贡献；用同一查询实时对比原始排序、重排后排序和去重后结果；用证据卡片显示 RAG 的查询改写、二跳检索、中选片段和最终答案之间的引用关系；展示 MRR 从 0.458、0.595 提高到 0.768 的消融实验；以人名、日期和跨页面课程交集为案例讲解失败原因与修复。AI 还建议将“结论不变性”作为一个独立的推理亮点：多个候选事实无法唯一消歧时，只要所有候选都导向相同结论，系统仍可给出稳定答案。本轮只进行了报告内容策划，没有修改搜索或 RAG 功能代码。

#### 38. 评估将 Embedding 接入当前检索系统

我提出将现有检索模型改为 Embedding 向量检索。AI 检查了统一搜索服务和本地语料规模：当前约有 1.59 万个本地 HTML 页面，现有流程使用 TF-IDF 生成前 500 个候选，再利用标题、摘要、发布日期和全文覆盖率重排，最后执行规范 URL、内容指纹和结果族去重。

AI 认为 Embedding 对长自然语言问题、近义表达和 RAG 召回会明显有帮助，但不建议直接删除现有稀疏检索。人名、课程名、年份、站次和精确 URL 等查询通常更依赖字面匹配，纯向量检索可能将“语义相似但事实对象不同”的页面排到前面。更稳妥的方案是混合检索：TF-IDF 和向量检索分别召回候选，用 Reciprocal Rank Fusion 等排名融合方法合并，再复用现有元数据重排和去重流程。

AI 建议先为每个文档用“标题 + 摘要 + 清洗后正文”离线生成向量，搜索阶段各取稀疏和稠密候选的前 100 至 500 条进行融合；RAG 则可进一步使用段落级向量，避免整页导航文字稀释关键证据。实验应同时比较纯 TF-IDF、纯 Embedding 和混合检索的 MRR@20、召回率、延迟和索引体积，而不应在未评测时用新模型直接覆盖已达到 0.768 的检索基线。本轮只完成架构评估和实验设计，没有下载向量模型、生成向量或修改生产检索代码。

#### 39. 用当前搜索项目解释 Embedding 基础概念

我说明自己还不太会使用 Embedding。AI 因此没有立即修改检索代码，而是先将它解释为“把文本转换为可以计算语义距离的数字向量”，并将实际接入流程简化为四步：离线读取页面文本并生成文档向量；将向量与 URL 对应保存；查询时用同一模型生成查询向量；通过余弦相似度找出最相近的页面。AI 还将 TF-IDF 和 Embedding 的差异概括为“字面共现”与“语义接近”，并建议先在几条文本上做最小实验，确认能观察到近义问句相似度更高，再接入 1.59 万页语料和混合排名。本轮仅进行概念教学和学习路线规划，没有安装依赖、下载模型或修改项目代码。

#### 40. 解释变长文档如何变成保留语义的定长向量

我进一步询问，不同长度的文档如何能转换为定长向量，同时保留语义。AI 将过程拆分为分词、词元初始向量、Transformer 上下文编码和 pooling 四个阶段。文档长度决定中间会产生多少个词元向量，但每个词元向量的隐藏维度由模型固定；平均池化、特殊汇总词元或加权池化再将任意数量的词元向量聚合为一个固定维度的文档向量。

AI 特别说明，Embedding 不是对原文的无损压缩，定长向量不可能保留文档的每个细节。语义能够在有限维度中保留，来自模型的训练目标：使语义相近的查询和文档向量靠近，使无关或容易混淆的文本向量远离。因此它保留的是模型在检索训练中学到的主题、实体关系、意图和语义特征，而不是可完整还原原文的编码。对超过模型上下文上限的长网页，AI 建议不做粗暴的整页单向量，而是先按段落分块，分别生成向量并在查询时取最相关片段。本轮只进行原理解释，没有修改代码或生成项目向量。

#### 41. 了解 Resiliparse 的网页正文提取用法

我询问 Resiliparse 如何使用。AI 查阅了 Resiliparse 官方文档和源码仓库，说明它是一套面向大规模网页与网页归档处理的高性能工具。对当前项目最有关的是 `resiliparse.extract.html2text.extract_plain_text()`，它可以从 HTML 中提取可见文本，并通过 `main_content=True` 执行基于规则的导航、侧边栏、页脚和其他模板噪声过滤。`HTMLTree.parse()` 适用于已解码字符串，原始字节则可以结合编码检测使用 `HTMLTree.parse_from_bytes()`。

AI 建议先将 Resiliparse 作为独立的正文提取实验，对同一批本地 HTML 比较当前 BeautifulSoup 选择器方案和 `extract_plain_text(main_content=True)` 的正文完整性、导航噪声、处理速度与 RAG 问题的证据召回。不建议在未对比前直接覆盖现有 `SearchService.get_page_content()`，因为当前代码针对课程语料的特定正文 CSS 容器已有专门回退逻辑，而 Resiliparse 的主内容抽取是通用规则。本轮仅说明安装、最小 API、本地字节编码处理和接入策略，没有安装新依赖或替换项目正文提取代码。

#### 42. 区分 Embedding 语料、训练数据和评测标注

我回到 Embedding 方案，并询问当前项目是否没有训练数据。AI 检查并区分了三类数据：项目有约 15886 个已下载网页，它们是待检索语料；项目曾使用课程验证查询和部分人工标注 URL 计算 MRR，这些属于验证或评测数据；但当前没有规模足够的“查询—相关文档—困难负例”配对，因此不具备从头训练或系统微调检索 Embedding 模型的专用训练集。

AI 说明，这不会阻塞第一版向量检索。可以直接使用已在大规模语义匹配数据上训练好的中文或多语种 Embedding 模型，仅对本地文档和查询做推理，不更新模型参数。少量已标注查询应当保留用于比较纯 TF-IDF、零样本 Embedding 和混合检索，不应同时拿来调参和报告最终效果。若以后需要领域微调，再从搜索日志、人工相关性标注或教师数据中构造正负文档对，并与最终测试集严格分离。本轮只进行数据角色和实验边界说明，没有训练、微调或生成文档向量。

#### 43. 审查 embedding_builder 的可运行性

我询问当前编写的 `ruc_search/embedding_builder.py` 是否可以直接运行。AI 检查了该脚本、新增的 `page_content.py`、输入路径和现有向量文件。脚本能通过 Python 语法编译，`docID.jsonl` 中的 `docID`、`file` 和 `url` 字段也与代码一致。AI 最初误用 `conda ML` 环境检查依赖，并因此错误报告 `sentence_transformers` 和 `resiliparse` 未安装；我随后指出该项目实际使用 `conda html`。AI 在正确环境中重新核对，确认已安装 `sentence-transformers 5.6.0`、`resiliparse 1.0.9` 和 `numpy 2.4.6`，因此导入依赖本身不是当前运行阻塞。

AI 还发现，当前名为 `build_embedding()` 的函数实际只读取网页、按 384 个 token 并以 64 token 重叠分块，最后写入 `chunks.jsonl`；它没有调用 `model.encode()`，没有生成或保存 chunk 向量，也没有建立向量行号与 `chunk_id` 的明确映射。函数依赖主程序中才定义的全局 `project_root`，并将所有分块先累积在内存后一次性写文件，因此不适合中断恢复，也难以观察 1.59 万个页面的处理进度。

项目中已有一个 `document_embeddings.npy`，其形状为 `(15886, 512)`、类型为 `float32`、体积约 31 MiB，抽查行向量的范数均为 1 且数值有限，看起来是有效的归一化文档向量。但当前源代码中没有生成它的 `model.encode()` 逻辑，该文件行数与页面数一致而不是与 chunk 数一致，因此它不能直接当作新 `chunks.jsonl` 的分块向量使用。AI 本轮只进行了只读审查、向量文件抽查和语法编译，没有下载模型、生成全库分块或覆盖现有向量。

#### 44. 设计 chunk encode 和消除 project_root 全局依赖

我纠正环境判断后，进一步询问 `model.encode()` 应该如何写，以及 `build_embedding()` 隐式依赖全局 `project_root` 的问题应如何解决。AI 建议保持 `all_chunks` 的列表顺序与 `chunk_id` 递增顺序一致，将其中的 `text` 按批交给 `SentenceTransformer.encode()`，设置 `convert_to_numpy=True` 和 `normalize_embeddings=True`，并将返回的二维 `float32` 数组写入独立的 `chunk_embeddings.npy`。在此顺序约定下，向量第 (i) 行对应 `chunks.jsonl` 中 `chunk_id = i` 的记录。

对于路径问题，AI 建议不再让函数读取主程序才创建的全局变量，而是在 `build_embedding()` 签名中显式增加 `project_root: Path`、`model: SentenceTransformer` 和 `embedding_path: Path` 参数，由 `if __name__ == "__main__"` 部分统一构造后传入。读取 `state["file"]` 时先判断是否为绝对路径，只对相对路径拼接项目根目录。本轮给出了可粘贴的参数传递和批量 encode 参考代码，但按我的问法没有直接改写 `embedding_builder.py`。

#### 45. 修正中文 chunk 被 tokenizer.decode 拆成空格单字的问题

我发现生成的 `chunks.jsonl` 中，`text` 的中文内容几乎全部变成了以空格分隔的单字，并询问这是否合理。AI 抽查了已生成的 114.44 MiB chunk 文件，前三个分块分别出现 339、268 和 316 处“中文字—空格—中文字”模式，确认这不是原网页排版，而是将 WordPiece 的 token IDs 通过 `tokenizer.decode()` 还原时插入的分隔空格。

AI 说明，虽然再次分词时部分中文模型可能仍能得到相近 token，但这种文本会破坏原文展示、字符串匹配和证据引用，也使“分块后再编码”不再严格等价于对原文分块。建议使用 fast tokenizer 的 `return_offsets_mapping=True`，依据每个 token 在原始字符串中的起止偏移直接切片原文，而不调用 `decode()`。修正后必须重新生成 `chunks.jsonl` 和对应的 chunk embeddings，以保证向量行号、`chunk_id` 和文本一致。抽查还发现第一个 chunk 主要由导航栏组成，这与 `page_content.py` 当前使用 `main_content=False` 相符，后续应单独评估是否为 Embedding 语料开启主内容去噪。本轮只进行了文件抽查和修正方案说明，没有直接修改分块代码或删除已生成文件。

#### 46. 说明 token 偏移与原文字符偏移

我对修正代码中的 `char_start` 含义提出疑问。AI 说明，`token_start` 和 `token_end` 表示 chunk 在 tokenizer 输出 token 序列中的半开区间，而 `char_start` 和 `char_end` 表示同一 chunk 在原始 Python 字符串中的半开字符区间。fast tokenizer 返回的 `offset_mapping[i] = (a, b)` 表示第 (i) 个 token 来自原文 `text[a:b]`；因此一个 token 区间的原文可通过 `text[offsets[start][0]:offsets[end - 1][1]]` 精确取回。保存字符偏移不是生成 Embedding 的必要条件，但有助于原文引用、证据高亮、检查 chunk 覆盖和从页面正文中重建片段。本轮只进行概念解释，没有修改源代码。

#### 47. 用实际 tokenizer 输出解释 encoded 和 offsets

我继续询问 `encoded` 和 `offsets` 分别是什么。AI 使用当前 `conda html` 环境中已缓存的 `BAAI/bge-small-zh-v1.5` tokenizer，对示例文本“许洪腾老师讲授机器学习”进行了一次不联网的实际分词。`encoded` 是 tokenizer 返回的字典式 `BatchEncoding`，本次包含 `input_ids`、`token_type_ids`、`attention_mask` 和 `offset_mapping`；分块逻辑主要使用其中的 `input_ids` 与 `offset_mapping`。

实际输出中，“许”对应 token ID 6387 和字符区间 `(0, 1)`，“洪”对应 ID 3825 和 `(1, 2)`，“腾”对应 ID 5596 和 `(2, 3)`；完整 11 个中文字在该 tokenizer 下得到 11 个 token。AI 说明，`input_ids` 是模型词表中的整数编号，是模型实际接收的输入；`attention_mask` 用来区分有效 token 与批处理时的填充位置；`token_type_ids` 主要用于区分句子段；`offset_mapping` 只是 tokenizer 额外返回给程序的原文坐标，不是送入 Embedding 模型的语义特征。本轮只对一条示例文本运行 tokenizer，没有生成或覆盖全库 chunk 和向量。

#### 48. 讨论 JSON 与索引产物的存放目录

我询问是否适合将项目中的所有 JSON 文件统一存入一个 `static` 目录。AI 说明，若这里指 Flask 或 Web 应用的 `static/`，则不应将后端倒排索引、URL 映射、页面元数据、chunk 记录和向量索引放入其中，因为该目录的语义是供浏览器直接获取 CSS、JavaScript、图片等前端资源，大型后端数据放在其中会带来误暴露、误下载和部署体积问题。

AI 建议按数据生命周期而不是按文件扩展名组织目录：抓取的 HTML 和 `docID.jsonl` 属于原始语料；`page_metadata.jsonl` 和 `chunks.jsonl` 属于可重建的处理产物；倒排索引和 `.npy` 向量属于索引产物；只有页面前端确实需要公开请求的小型 JSON 才应放入 Web `static/`。如果只是想表示“固定生成的数据”，使用 `data/`、`artifacts/` 或 `indexes/` 等名称比 `static/` 更清晰。本轮只提供目录设计建议，没有移动或删除任何数据文件。

#### 49. 验证全库 chunk 与 Embedding 产物

我完成了 Embedding 构建并将相关数据产物整理到 `data/` 目录。AI 对 `docID.jsonl`、`chunks.jsonl`、`chunk_embeddings.npy` 和本地 HTML 进行了只读完整性检查。结果显示，15886 个网页共生成 75778 个 chunk；向量矩阵形状为 `(75778, 512)`、类型为 `float32`、体积约 148 MiB，行数与 chunk 数完全一致。

AI 遍历了全部 chunk 元数据，确认 `chunk_id` 从 0 起连续递增，没有发现 ID 与行号不一致的记录；每条记录均包含 `doc_id`、URL、标题、token 起止位置、字符起止位置和原文。前 20 个 chunk 中的中文逐字空格问题已基本消失，说明基于 `offset_mapping` 的原文切片已生效。AI 还抽查了向量矩阵的首行、中间行和末行，向量范数均为 1，且所有数值均为有限值；`docID.jsonl` 中抽查的相对 HTML 路径也能够从项目根目录正确解析。这表明当前数据已满足实现 chunk 向量检索的基本一致性要求。本轮没有重新编码、删除或覆盖任何数据文件。

### 三、AI 建议的采纳与待处理情况

目前已采纳的建议包括：

- 为索引输出文件自动创建父目录；
- 将构建、保存和加载倒排索引拆分为独立方法；
- 在索引已存在时尝试加载，而不是直接返回；
- 将 docID 改为从 0 开始。
- 统一索引的保存与加载格式，并在加载后恢复 `defaultdict`；
- 使用停用词表、`strip()` 和 `casefold()` 过滤并归一化词项；
- 完成单词查询、逻辑与查询和逻辑或查询；
- 将日常运行的 `remake` 设为 `False`。

仍待确认或处理的项目包括：

- 处理路径对当前工作目录的依赖；
- 将 `query_OR()` 的异常提示中误写的 `query_AND` 改为 `query_OR`；
- 确认并补齐 `conda ML` 环境中的 HTML 解析依赖。

### 四、使用说明与声明

AI 在本次作业中主要承担代码审查、异常原因分析、文件格式检查和修改建议的作用。我根据作业要求理解并选择是否采纳这些建议，并对最终代码和运行结果负责。

后续每次就 Day 3 作业与 AI 进行实质交流后，本文档将继续按实际交流和采纳情况更新。

---

## Day 4 Task：AI 使用记录

### 一、使用目的

在完成 Day 4 Task 的过程中，我使用 AI 辅助阅读课件和配套 notebook，理解词频、文档频率、tf-idf 加权和向量空间模型，并在 Day 3 倒排索引的基础上规划和检查 Day 4 的实现。

本次协作以课件内容梳理、实现步骤设计、稀疏存储方案讨论、代码审查和小规模数值验证为主。AI 没有直接修改 `Inverted_index.py`，代码的具体实现和修改由我完成。

### 二、具体使用情况

#### 1. 阅读 Day 4 课件和配套代码

我向 AI 提供了 Day 4 的任务要求，并请它查看 `day4-排序检索.pdf` 和 `倒排索引构建和余弦相似度计算_new.ipynb`。AI 逐页检查了课件，并提取了 notebook 中的代码单元。

AI 将任务归纳为以下过程：

- 在 postings 中保存词项在文档中的词频 \(tf\)；
- 为每个词项保存文档频率 \(df\)；
- 使用对数词频和逆文档频率计算 tf-idf 权重；
- 保存每篇文档的 tf-idf 向量长度；
- 将查询也表示为 tf-idf 向量；
- 利用倒排索引累加点积，再计算余弦相似度并返回 Top-K 结果。

AI 特别指出，课件 notebook 最后的 Python 示例明确没有考虑 idf，只能作为余弦得分程序的起点，不能直接作为 Day 4 的最终实现。

#### 2. 设计改进后的倒排索引结构

AI 建议不再使用 Day 3 的纯 docID 列表，而是为每个词项保存 `df`、`idf` 和 postings，每个 posting 保存 `docID` 和 `tf`。索引顶层另外保存文档数量和每篇文档的向量长度。

建议的 JSON 结构类似：

```python
{
    "document_count": 15947,
    "terms": {
        "人工智能": {
            "df": 120,
            "idf": 2.12,
            "postings": [
                {"docID": 3, "tf": 2},
                {"docID": 15, "tf": 4}
            ]
        }
    },
    "doc_lengths": [12.34, 8.91]
}
```

这种结构可以直接使用 JSON 保存，也能保留 Day 4 排序检索所需的全部信息。

#### 3. 制定分步实现计划

AI 将实现拆分为多个可单独检查的阶段：

1. 新建独立的 `hw/day4` 模块，不覆盖 Day 3；
2. 统一文档和查询的分词、大小写归一化和停用词过滤规则；
3. 使用 `Counter` 按文档统计 \(tf\)；
4. 在每篇文档每个词项只生成一个 posting 的基础上计算 \(df\)；
5. 计算 idf 和每篇文档的 tf-idf 向量长度；
6. 实现索引的原子保存和加载；
7. 调整 Day 3 的 AND 和 OR 查询，使其适配新 postings 结构；
8. 建立查询 tf-idf 向量；
9. 基于查询词的 postings 稀疏累加点积；
10. 进行余弦归一化、排序并返回 Top-K；
11. 先用人工构造的小数据测试，再在 Day 2 的 15947 篇网页上重建索引。

#### 4. 讨论 tf-idf 和余弦相似度公式

根据课件，AI 建议使用对数词频：

​\[
w_{tf}(t,d)=
\begin{cases}
1+\log_{10}(tf_{t,d}), & tf_{t,d}>0,\\
0, & tf_{t,d}=0.
\end{cases}
\]

逆文档频率为：

​\[
idf_t=\log_{10}\left(\frac{N}{df_t}\right).
\]

文档中词项的 tf-idf 权重为：

​\[
w_{t,d}=\left(1+\log_{10}(tf_{t,d})\right)idf_t.
\]

文档向量长度为：

​\[
L_d=\sqrt{\sum_t w_{t,d}^2}.
\]

完整的余弦相似度为：

​\[
\cos(q,d)=
\frac{\sum_t w_{t,q}w_{t,d}}{L_qL_d}.
\]

AI 说明，课件示例在排序时只除以文档长度，是因为对固定查询而言 \(L_q\) 是常数，省略它不会改变排序。但为了返回真正的余弦相似度，最终实现应同时除以 \(L_q\) 和 \(L_d\)。

#### 5. 第一次检查 Day 4 部分实现

我首先实现了新数据结构、文档遍历、词频累加和文档频率计算。AI 检查后认为整体方向正确，但指出：

- `docID` 字段被误写为 `docIC`，会在第一条记录处报错；
- `add_term()` 使用空的 `except` 区分首次插入，会隐藏其他程序错误；
- 主程序没有传入 `stopwords.txt`，因此实际构建时不会过滤停用词；
- 建议用 `Counter` 先统计一篇文档内的词频，再为每个不同词项生成一个 posting。

AI 使用小数据独立测试了当时的 `add_term()`，确认它在不考虑上述异常处理问题时，能将同一文档中的两次出现记为 \(tf=2\)，并将两篇包含该词的文档记为 \(df=2\)。

#### 6. 讨论构建和查询效率

我询问当前方案可以进行哪些效率优化。AI 认为收益最大的优化是将“完整重建”和“从文件加载”分开，避免每次创建对象都重新解析 15947 个 HTML 文件。

AI 还建议：

- 使用 `Counter` 降低内层循环中的字典查询和异常处理开销；
- 构建 postings 时同时累加 \(df\)，避免额外遍历；
- 所有文档处理完后统一计算 idf；
- 从 postings 累加每篇文档的权重平方，无需重新打开 HTML；
- 查询时只遍历查询词对应的 postings，不遍历全部文档；
- 只需返回前 \(k\) 个结果时，使用 `heapq.nlargest()` 代替对所有候选结果完整排序。

AI 没有建议在当前阶段立即加入多进程 HTML 解析，因为这会增加 jieba 初始化、进程间传输、postings 顺序恢复和异常处理的复杂度。

#### 7. 讨论文档向量的稀疏存储

我担心为每篇网页保存一个完整词表维度的向量会占用过多内存。AI 说明，不应保存稠密向量。当文档数约为 15947、词表大小约为 170665 时，稠密矩阵约有

​\[
15947\times170665\approx2.72\times10^9
\]

个元素，仅使用 64 位浮点数也需要约 21.8 GB，还没有计算 Python 对象开销。

AI 说明，倒排索引的 postings 本身就是文档向量的稀疏表示。任务只需保存每个词项的 postings 以及每篇文档的一个长度值，查询时只读取查询词对应的 postings 并累加点积，无需显式实例化任何完整高维向量。

#### 8. 检查 `Counter`、idf 和文档长度实现

我将文档词项改为先经过 `normalize_words()` 处理，然后使用 `Counter` 统计，并实现了 idf 和 `calc_doc_lengths()`。AI 检查后确认：

- `Counter` 能正确计算每篇文档中每个词的 \(tf\)；
- 每篇文档每个词项只添加一个 posting，因此每次添加 posting 时增加 \(df\) 是正确的；
- 所有文档处理后再根据最终文档数和 \(df\) 计算 idf，时机正确；
- `calc_doc_lengths()` 通过 postings 累加 tf-idf 权重平方，没有构建稠密向量。

AI 使用三篇模拟文档对 `calc_doc_lengths()` 进行了数值验证，计算结果与手工公式一致。

在这次检查中，AI 还发现了以下工程问题：

- 当时 `docID` 仍被误写为 `docIC`；
- 主程序将 `stopwords_path` 当成了构造函数的 `index_path` 传入；
- 构建并保存后立即将文档数、词项字典和文档长度清空；
- 保存时没有先创建输出文件的父目录；
- 保存或加载失败时只打印错误，可能让一个不完整的对象继续运行。

#### 9. 复查修正后的当前版本

我根据检查结果继续修改代码。AI 复查最新版本后确认：

- `docIC` 已修正为 `docID`；
- 主程序已分别传入 docID 路径、索引路径和停用词路径；
- 保存前已自动创建父目录；
- 保存和加载失败后已重新抛出异常；
- 构建完成后清空对象状态的代码已删除；
- 当前 `Inverted_index.py` 已通过 Python 语法编译检查。

当前尚未生成 `inverted_index/inverted_index.json`，因此 AI 没有代替我触发 15947 篇网页的完整重建。目前可以确认数据结构、\(tf\)、\(df\)、idf 和文档长度的算法已通过代码审查和小数据数值验证；真实索引的完整性、文档数、postings 范围和保存后加载一致性仍需在首次构建成功后验收。

#### 10. 明确查询时的稀疏余弦计算方式

我询问查询时是否需要对每个 HTML 网页分别计算一次余弦相似度。AI 说明，查询阶段不应再打开或解析任何 HTML，也不应遍历全部 15947 篇文档。索引构建阶段已将检索所需的 \(tf\)、\(df\)、idf、postings 和文档向量长度保存下来。

查询时只需进行以下操作：

1. 对查询分词，并计算查询词频和 tf-idf 权重；
2. 对每个查询词，只读取该词的 postings list；
3. 将这些 postings 中出现的 docID 作为候选文档；
4. 通过 `scores[docID]` 累加查询向量和文档向量的点积；
5. 只对 `scores` 中的候选文档使用已保存的文档长度进行余弦归一化；
6. 选取得分最高的 Top-K 结果。

因此，候选文档集合实际上是各查询词 postings list 中 docID 的并集。完全不包含查询词的文档点积为 0，不需要显式计算。

#### 11. 检查初步的 tf-idf 查询实现

我实现了查询分词、候选文档得分累加、文档长度归一化和 Top-K 截取，并请 AI 检查当前 Day 4 代码。AI 确认当前查询已经只遍历查询词对应的 postings，没有重新打开 HTML，稀疏检索方向正确。

但 AI 指出，当前得分分子中的查询词权重和文档词权重都只使用了对数 \(tf\)，没有乘以该词的 idf；而已保存的文档长度却是使用 tf-idf 权重计算的。这会导致分子和分母使用不一致的向量表示，得到的数值不是 tf-idf 余弦相似度。

AI 建议对每个查询词使用相同的 idf 同时计算查询权重和文档权重，并累加查询向量长度：

​\[
w_{t,q}=\left(1+\log_{10}(tf_{t,q})\right)idf_t,
\]

​\[
w_{t,d}=\left(1+\log_{10}(tf_{t,d})\right)idf_t.
\]

得分最后应同时除以查询长度和文档长度。AI 还使用一个 idf 为 0 的模拟词项进行边界测试，确认当前代码会在文档长度为 0 时触发 `ZeroDivisionError`。因此，归一化前需要检查查询长度和文档长度是否为 0。

此外，AI 指出当前主程序仍固定使用 `remake=True`，正式索引生成后应改为 `False`；查询函数还应检查查询是否为字符串以及 \(k\) 是否为正整数。在 `conda ML` 环境中进行完整导入检查时，还发现该环境目前缺少 `jieba`。

#### 12. 复查查询权重修正后的版本

我在查询中加入了 idf、查询向量长度和零长度检查，并再次请 AI 复查。AI 确认查询参数检查、tf-idf 权重计算和文档零长度过滤的方向已正确，但发现两个仍会阻止正确查询的问题。

第一，直接遍历 `Counter` 只会得到词项 key，不会得到 `(词项, 次数)` 二元组。当前代码写成 `for term, query_tf in query_terms`，查询英文单词时会触发 `ValueError: too many values to unpack`。应改为遍历 `query_terms.items()`。

第二，当前代码已计算并开方得到查询向量长度，但最终得分仍然只除以文档长度。若要返回完整余弦相似度，还需要同时除以查询向量长度。省略它不会改变同一查询下的文档排序，但返回数值不是完整的余弦值。

#### 13. 排查教师列表页中的相对链接是否漏爬

我在 Day 4 测试中怀疑爬虫没有从 `http://ai.ruc.edu.cn/academicfaculty/szdwn/index.htm` 发现 `wjr/index.htm`，因而没有抓取文继荣老师的中文个人页。AI 对本地抓取数据、HTML、docID 映射和 Day 4 索引进行了逐层检查。

检查结果表明，该页面并未漏爬：

- 教师列表页已保存，对应 docID 9183；
- 本地保存的列表页 HTML 中确实包含 `href="wjr/index.htm"` 的姓名链接和“中文”按钮链接；
- 以列表页 URL 为基址调用 `urljoin()`，可正确得到 `http://ai.ruc.edu.cn/academicfaculty/szdwn/wjr/index.htm`；
- 目标页已出现在 `url_index.jsonl` 和 `docID.jsonl` 中，对应 docID 2503；
- 目标 HTML 文件已保存到本地，其标题、一级标题和正文包含“文继荣”和“信息检索”等内容；
- Day 4 索引的“文继荣”词项 postings 中同时包含目标页 docID 2503 和列表页 docID 9183；
- 两篇文档都已计算出非零 tf-idf 向量长度。

AI 进一步按当前 tf-idf 余弦公式对“文继荣”进行单词排名核对。目标个人页 docID 2503 中该词的 \(tf=3\)，余弦得分约为 0.0866，在候选文档中排名第 68；列表页 docID 9183 排名第 505。因此，默认只返回 Top 10 时不会看到目标页，这是排名和截断结果，不是爬取或建索引失败。

AI 还说明，当前文本索引只提取 `title`、`h1`至 `h3` 和 `p` 中的文字，不会把链接 URL 或所有锚文本作为目标文档的内容，链接关系本身也不会在当前向量空间模型中产生排名加成。

#### 14. 更正目标页面并检查重定向 URL 的记录

我随后更正说明，实际怀疑缺失的页面是 `https://gsai.ruc.edu.cn/addons/teacher/index.html`，而不是上一节检查的文继荣个人页。AI 重新检查了本地的 `url_index.jsonl`、`docID.jsonl` 和已保存 HTML，并联网核对了 URL 的跳转结果。

检查发现，精确 URL `https://gsai.ruc.edu.cn/addons/teacher/index.html` 的确没有出现在本地 URL 索引和 docID 映射中，但该页面的 HTML 内容已经抓取并保存在 `https://gsai.ruc.edu.cn` 对应的文件中，文件标题为“教师主页系统”，正文中也包含教师列表及各教师主页链接。

原因是访问种子 URL `https://gsai.ruc.edu.cn` 时，网站会经过一次 HTTP 重定向，最终到达 `https://gsai.ruc.edu.cn/addons/teacher/index.html`。当前爬虫的 `requests.get()` 会自动跟随重定向，但 `get_html()` 只返回响应正文；之后 `save_html(url, html)` 仍使用请求前的原始 URL 生成文件名和索引记录，没有保存最终响应 URL `response.url`。所以这是重定向后的规范 URL 没有被记录，不是页面内容没有下载。

AI 建议若要让 URL 索引准确反映最终地址，应让下载函数同时返回 HTML 与最终 URL，并在保存、去重和链接基址处理中统一考虑最终 URL。若作业只要求网页内容能够参与倒排索引和检索，当前该教师列表页的内容已经进入语料；若还要求 URL 映射准确，则需要再修正重定向处理逻辑。

#### 15. 排查 HTTP 与 HTTPS 页面重复收录

我发现同一页面可能同时以 HTTP 和 HTTPS 两种地址进入语料，例如 `http://gsai.ruc.edu.cn/addons/video/video/play.html?id=2` 和对应的 HTTPS 地址。AI 检查本地 `url_index.jsonl` 与 `docID.jsonl` 后确认，这两个地址分别保存为不同文件，并分别获得 docID 3037 和 1635。按主机名、路径和查询参数相同而仅协议不同的条件统计，`gsai.ruc.edu.cn` 中共有 107 组此类记录。

AI 联网核对示例地址后确认，HTTP 地址会经过一次重定向，最终到达相同的 HTTPS 地址，而直接访问 HTTPS 地址不会发生重定向。因此这些记录在语义上是同一页面的重复抓取。出现重复的原因仍是爬虫只根据抓取前的 URL 字符串维护 `all_links`、生成文件名和写入 URL 索引，没有使用服务器响应中的最终 URL `response.url` 进行规范化和去重。

AI 不建议无条件把所有网站的 HTTP 地址改写成 HTTPS，因为并非所有主机都保证两种协议可用且内容等价。更稳妥的方案是让下载函数返回响应正文和最终 URL，将最终 URL 规范化后用于保存、解析相对链接和已保存页面去重，并维护原始 URL 到最终 URL 的重定向关系。对于现有数据，若清除重复记录，还需要重新生成连续的 docID 映射并重建 Day 4 倒排索引，不能只删除一个 HTML 文件或一行 URL 记录。

#### 16. 检查下载函数返回最终 URL 后的配套修改

我将 `get_html()` 的成功返回值改为 `(response.content, response.url)`，并询问其他位置还需要如何调整。AI 检查当前代码后指出，调用处仍把整个返回值赋给 `html`，随后传给 `save_html()` 和 `ExtractHTML()`；若直接运行，写文件时会因为传入元组而不是字节串报错。

AI 建议在主循环中先判断下载结果是否为 `None`，再解包为 `html, final_url`，并统一使用 `final_url` 保存页面、解析相对链接和计算子链接优先级。同时应验证重定向后的主机名仍属于允许抓取的主机集合，避免 `requests` 自动跟随重定向后保存站外页面。

仅把最终 URL 加入现有 `all_links` 仍不足以可靠去重，因为 `all_links` 表示链接已经被发现或加入队列，并不表示页面已经成功下载。AI 建议另设 `saved_urls` 集合，从已有 `url_index.jsonl` 恢复，并在每次成功保存后更新。请求得到的 `final_url` 如果已经在 `saved_urls` 中，应跳过重复保存；从队列取出的 URL 如果已保存，也可以在发起请求前跳过。该修改只能防止以后新增重复，现有的 HTTP/HTTPS 重复记录仍需单独清理并重新生成 docID 和 Day 4 索引。

#### 17. 复查重定向去重修改并安排后续步骤

我完成配套修改后询问下一步。AI 对当前 `Crawler.py` 进行了差异检查和静态编译检查。代码语法可以通过编译，但发现 `self.saved_urls = self.load_saved_urls()` 被放在 `self.url_index_file` 初始化之前；调用 `load_saved_urls()` 时访问该属性会触发 `AttributeError`，因此需要将该语句移动到 `url_index_file` 赋值之后。

AI 还指出，当前在下载失败、重定向到站外或最终 URL 已保存时直接执行 `continue`，会跳过循环末尾按 `save_interval` 执行的断点保存。建议调整控制流，使这些情况不绕过定期保存逻辑，或者把定期保存封装为不会被提前跳过的步骤。另外可以在发起请求前检查队列 URL 是否已经位于 `saved_urls`，从而减少不必要的重复请求。

AI 建议修正后先使用 HTTP/HTTPS 两个示例地址和独立的临时 HTML 目录、临时断点文件进行小规模测试，确认最终只写入一个 HTTPS URL，再决定如何处理正式数据。现有正式断点的 frontier 已为空，直接使用旧目录和旧断点运行不会自动删除或重新整理旧的重复数据；若需要完全干净的语料，应先备份旧结果，再重新抓取，之后重新生成 docID 并重建 Day 4 索引。

#### 18. 确认重新抓取的启动方式

我确认备份完成后是否可以直接运行 `python -m day2.Crawler`。AI 说明，在前述初始化顺序和断点保存控制流问题修正后，应同时备份并移走原来的 `downloaded_html` 目录与 `crawler_checkpoint.json`，然后从 `hw` 目录进入 `conda ML` 环境运行该模块。这样爬虫会因找不到旧断点和旧 URL 索引而从种子 URL 重新开始，并创建新的抓取目录与索引。AI 同时建议先完成两网址的小规模测试，再开始耗时较长的完整抓取。

#### 19. 分析姓名查询中个人主页未排第一的原因

我使用“赵鑫”进行查询时，两个视频页面排在赵鑫个人主页之前，因此询问排序原因。AI 从实际 Day 4 索引中提取了三篇文档的词频、idf、文档向量长度和余弦分数进行核对。

索引中“赵鑫”的文档频率为 173，idf 约为 1.963。视频 52 页面中该词的 (tf=1)，文档长度约为 4.943，单词查询余弦分数约为 0.397；视频 30 页面中 (tf=1)，文档长度约为 6.345，分数约为 0.309；个人主页中 (tf=2)，文档长度约为 9.659，分数约为 0.264。因此当前程序给出的前三名次序与实现的 tf-idf 余弦公式一致。

AI 解释，对于只有一个有效词项的查询，查询向量归一化后，排序主要取决于该词的文档权重占整篇文档向量长度的比例，而不是只比较词频。个人主页虽然出现两次“赵鑫”，但还包含 44 个索引词项以及 `batmanfly`、`aibox`、`gmail`、科研项目和学术论文等高权重词，文档向量长度明显更大；短视频页面的索引词更少，“赵鑫”在向量方向中的占比反而更高。

AI 进一步检查 HTML 结构发现，个人主页中的“赵鑫”分别出现在 `<title>` 和 `<h1>` 中，而视频页面中的姓名主要出现在 `<h3>` 中。当前实现把标题、一级至三级标题和段落分词直接合并后统一统计，没有为页面标题、一级标题或姓名主页设置字段权重。因此个人主页未排第一是标准词袋余弦模型的长度归一化结果，不表示代码计算错误。若希望姓名查询优先返回个人主页，需要在基础作业之外加入标题字段加权、精确标题匹配加分或其他排序特征。

#### 20. 讨论标题、文档标题与正文的字段权重

我询问是否可以为 HTML 的 `title`、一级至三级标题和正文分别设置 5、2、1 的权重。AI 说明这种做法属于字段加权，可以作为基础 tf-idf 向量空间模型的扩展，但更准确的表述是将各字段中的词频乘以权重，而不是在最终检索分数上直接加固定分。

AI 建议可先分别对 `title_words`、`doctitle_words` 和 `body_words` 使用 `Counter`，再按 (5tf_{title}+2tf_{doctitle}+tf_{body}) 合成文档的加权词频，后续仍使用现有的对数 tf、idf 和余弦归一化流程。若同一词同时出现在页面标题和一级标题中，两部分贡献会相加，这正是字段加权希望表达的效果。

AI 同时提醒，这样保存的 `tf` 已经是加权词频而不是原始出现次数。如果作业要求明确保存真实词频，更严谨的做法是分别保存原始总词频和三个字段词频，再在评分阶段计算字段权重。无论采用哪种方式，文档向量长度必须按新的字段加权结果重新计算，否则分子和分母会使用不一致的表示。修改后需要以 `remake=True` 完整重建 Day 4 索引。

#### 21. 复查字段加权实现

我完成字段加权代码后请 AI 检查。AI 确认当前实现分别统计 `title`、一级至三级标题和正文词频，保存原始 `tf` 与 `weighted_tf`，并在文档向量长度和查询文档权重两处统一使用 `weighted_tf`，整体计算逻辑一致，静态编译能够通过。

AI 指出当前实际权重为 3、2、1，而不是上一轮讨论的 5、2、1；若这是为了减弱标题权重而有意选择，则没有问题。当前磁盘中的旧索引 postings 仍然只有 `docID` 和 `tf`，但主程序仍设置 `remake=False`，因此直接查询会在读取 `weighted_tf` 时触发 `KeyError`。下一步应先将 `remake` 临时改为 `True` 完整重建索引，完成后再改回 `False`。

运行环境检查还发现，项目约定使用的 `conda ML` 环境目前缺少 `bs4`、`html5lib` 和 `jieba`，模块导入会首先因缺少 `bs4` 失败。因此在该环境中重建索引前，需要先补齐这些依赖，或者确认此前实际运行索引程序所使用的 Python 环境。

### 三、AI 建议的采纳情况

目前已采纳的建议包括：

- 建立独立的 Day 4 模块，不覆盖 Day 3；
- 将词项归一化和停用词过滤封装为 `normalize_words()`；
- 使用 `Counter` 按文档统计词频；
- 在 postings 中保存 `docID` 和 `tf`；
- 为每个词项保存 `df` 和 `idf`；
- 使用 postings 稀疏计算文档 tf-idf 向量长度；
- 将索引构建与索引加载分开；
- 使用临时文件和 `os.replace()` 保存索引；
- 保存前创建父目录；
- 保存或加载失败时不再吞掉异常。

仍待完成或验证的部分包括：

- 首次构建并验收 Day 4 的真实索引文件；
- 调整并验证 AND 和 OR 布尔查询，使其读取新 postings 结构；
- 实现查询分词、查询 tf-idf 权重和查询向量长度；
- 基于查询词 postings 累加文档点积；
- 进行完整余弦归一化并返回 Top-K 结果；
- 对保存前后的索引和查询结果进行一致性测试。

### 四、使用说明与声明

AI 在本次作业中主要承担课件内容梳理、实现步骤设计、复杂度和存储方案分析、代码审查以及小数据数值验证的作用。我根据课件和作业要求理解并选择是否采纳建议，并对最终代码、索引数据和查询结果负责。

后续每次就 Day 4 作业与 AI 进行实质交流后，本文档将继续按实际交流、代码修改和验证结果更新。

---

## Day 5 Task：AI 使用记录

### 一、使用目的

在完成 Day 5 Web UI 和检索结果展示任务的过程中，我使用 AI 辅助阅读 Day 5 课件、检查最简 Web UI 实例是否符合任务要求，并讨论如何提高搜索结果标题和摘要获取过程的速度与稳定性。

本次协作以课件要求核对、代码审查、运行方式检查、问题分析和实现方案讨论为主。Day 5 的 Python、HTML 和说明文件均由我编写和修改，AI 没有直接修改这些代码文件。AI 根据我的要求协助整理并写入了本使用记录。

### 二、具体使用情况

#### 1. 对照 Day 5 课件检查最简实例

我完成了一个最简 Day 5 实例，并请 AI 检查需要改进的地方。AI 阅读了 `人工智能综合设计-day5-WebUI和评测.pdf`，将当天的主要要求归纳为：

- 实现搜索引擎 Web UI；
- 对给定的单个查询语句返回检索结果前 20 个 URL；
- 至少实现一个扩展功能；
- 注意文件组织、Git 使用、变量命名、缩进、接口设计和全局变量等代码风格；
- 不提交倒排索引、已爬取网页和其他中间结果；
- 在实验报告中说明扩展功能、模块设计、核心代码和问题解决过程。

AI 检查后认为，当前实例已经完成了从查询页面接收关键词、调用 Day 4 排序检索、返回前 20 个 URL，并在结果页面展示标题和摘要的基本流程。标题和摘要展示与课件中的扩展示例一致，可以作为一个扩展功能，但需要在实验报告中明确说明。

#### 2. 检查程序启动方式和模块导入

AI 在项目要求的 `conda ML` 环境中尝试检查程序。检查发现，最初 README 中的启动方式与项目的包结构不一致：直接执行 `python hw/day5/main.py` 或在 `day5` 目录执行 `python main.py` 时，Python 无法正确找到同级的 `day4` 包。

AI 建议从 `hw` 目录使用模块方式启动：

```bash
python -m day5.main
```

同时，AI 检查到当前 `conda ML` 环境缺少 Flask、Beautiful Soup 和 jieba，而 README 最初只列出了 Flask。AI 因此建议补充实际依赖和正确的启动说明。README 的具体修改由我完成。

#### 3. 检查自动评测接口和 Flask 启动位置

课件要求提供一个能够对单个查询返回前 20 个 URL 的函数。最初版本只在 Flask 的 `/query` 路由中直接调用 `search_engine.query(key, k=20)`，没有将这一功能封装为独立接口。

AI 建议增加一个独立的搜索函数，使 Web 路由和自动评测都可以调用同一接口，并固定返回前 20 个 URL。AI 还指出，最初的 `app.run()` 位于模块顶层，其他程序导入该模块时会立即启动并阻塞 Flask 服务，因此应将服务器启动放在主程序保护中。

我根据任务需要编写了独立的 `search()` 函数，并将 Flask 启动代码放入：

```python
if __name__ == "__main__":
    ...
```

这些代码由我编写和修改，AI 仅提供了接口设计和代码组织方面的建议。

#### 4. 分析在线获取标题和摘要的问题

最初版本在每次查询得到 URL 后，使用 `requests.get()` 依次访问最多 20 个网页，并从返回的 HTML 中提取标题和摘要。AI 指出，这种实现虽然直观，但存在以下问题：

- 每次搜索都需要重新访问外部网站，响应速度受网络状态影响；
- 20 个网页依次请求，每个请求的超时时间为 10 秒，最坏情况下等待时间可能很长；
- 任何一个网页超时、拒绝访问或返回错误，都可能导致整个搜索结果页面报错；
- 外部网页内容可能发生变化，展示内容与建立索引时使用的本地网页版本不一致；
- 重复查询相同结果时会重复下载相同页面，浪费时间和网络资源。

AI 建议不要在查询请求中实时联网获取元数据，而是在索引或预处理阶段直接从 Day 2 已下载的本地 HTML 中提取标题和摘要，并保存为本地元数据文件。查询阶段只需按 URL 查表，不再访问外网。

#### 5. 讨论本地网页元数据的保存格式

为了给出适合现有项目的方案，AI 检查了 `downloaded_html/docID.jsonl`、本地 HTML 文件布局和 Day 1 的 `ExtractHTML.py`。当前 `docID.jsonl` 的每条记录已经保存 `docID`、原始 URL 和本地 HTML 文件路径，因此可以直接作为离线元数据提取的输入。

AI 建议新增一个 JSONL 元数据文件，每行对应一篇文档，保存以下字段：

```json
{"docID": 0, "url": "https://example.com/", "title": "页面标题", "abstract": "页面摘要"}
```

选择 JSONL 的原因是它与现有 `docID.jsonl` 的格式一致，可以逐行处理，不需要在生成时将全部文档记录同时保存在内存中。查询程序启动时可以再将其加载为以 URL 为键的字典，因为当前 Day 4 的查询函数返回 URL 列表。

AI 建议将文件保存为 `inverted_index/page_metadata.jsonl`。该目录已经在 `.gitignore` 中，因此不会将本地网页的处理结果误提交到 Git，符合课件对中间结果的要求。

#### 6. 讨论标题和摘要的提取规则

AI 建议标题按照以下优先级提取：

1. `citation_title`；
2. `og:title`；
3. `twitter:title`；
4. HTML 的 `<title>`；
5. 如果以上内容都不存在，则回退为页面 URL。

摘要按照以下优先级提取：

1. `citation_abstract`；
2. `description`；
3. `og:description`；
4. `twitter:description`；
5. 如果页面没有摘要元数据，则拼接段落文本，并截取正文前 200 个字符；
6. 如果仍然没有可用文本，则保存空摘要，在 Web UI 中显示“暂无摘要”。

AI 还建议将连续空白字符合并，避免网页中的换行和缩进直接出现在搜索结果中；单个网页解析失败时应记录失败信息并继续处理其他网页，而不是终止整个元数据生成过程。

#### 7. 编写离线元数据生成程序

在讨论上述方案后，我编写了 `day5/build_page_metadata.py`。该程序完成以下工作：

- 逐行读取 `downloaded_html/docID.jsonl`；
- 根据记录中的本地文件路径读取 HTML 字节；
- 使用 Beautiful Soup 提取标题和摘要；
- 对标题、摘要中的连续空白进行清理；
- 页面缺少描述时使用正文片段作为摘要；
- 单个文件处理失败时保存 URL 和空摘要，并继续处理；
- 将结果逐行写入 `page_metadata.jsonl`；
- 先写入临时文件，全部完成后再替换正式文件，避免中途中断留下不完整结果；
- 输出成功和失败的文档数量。

AI 提供了数据流设计、字段建议、提取优先级、异常回退和原子保存的参考方案。我理解方案后完成了当前代码文件，最终代码由我编写并负责。

#### 8. 将本地元数据接入 Web UI

我修改了 `day5/main.py`，在 Flask 服务启动前读取 `page_metadata.jsonl`，并建立从 URL 到元数据记录的字典。搜索时仍先调用 Day 4 的检索函数获得前 20 个 URL，然后直接从字典中取得标题和摘要。

若某个 URL 没有对应元数据，当前实现使用 URL 本身作为标题；若摘要为空，则显示“暂无摘要”。因此，查询阶段不再对结果网页发起网络请求，也不会因为某个外部网站暂时不可访问而导致整个结果页面失败。

AI 同时建议使用 `Path(__file__).resolve()` 构造项目内文件的绝对路径，避免程序依赖当前工作目录。我在当前版本中使用 `PROJECT_ROOT` 统一构造 docID、倒排索引、停用词表和页面元数据路径。

#### 9. 其他代码审查建议

AI 在检查最简实例时还提出了以下后续改进建议：

- 删除未使用的变量和导入；
- 对空查询和全空白查询进行统一处理；
- 在结果页面增加无结果提示、结果数量、查询耗时和新的搜索框；
- 为独立搜索函数增加空查询、无命中、结果不足 20 条和结果超过 20 条等测试；
- 本机演示时优先监听 `127.0.0.1`，谨慎使用 `0.0.0.0` 和 Flask 调试模式；
- 在 README 中说明离线元数据生成和 Web UI 启动的先后顺序；
- 在最终提交前生成并验证 Day 5 Git bundle。

这些建议中，与自动评测接口、主程序保护、本地元数据、路径构造和模块启动有关的部分已经由我在当前版本中实现；其余界面、测试、依赖说明和提交检查可以继续完善。

#### 10. 协助编写 Web UI 样式和页面前端

我进一步询问如何美化当前较为简洁的搜索界面。AI 根据现有 Flask 模板结构，协助设计了一套不依赖外部前端框架的搜索引擎界面，并提供了 CSS 样式、主页模板和结果页模板的参考代码。

AI 建议在 `day5/static/style.css` 中集中管理页面样式，使 Flask 通过静态文件目录自动加载 CSS。样式设计主要包括：

- 使用统一的主题色、正文颜色、弱化文字颜色、边框颜色和页面背景色；
- 首页采用垂直、水平居中的搜索区域；
- 为搜索框增加圆角、阴影、聚焦边框和聚焦光晕；
- 为搜索按钮增加主题色和悬停状态；
- 在结果页顶部保留站点名称和搜索框，方便用户直接修改查询；
- 使用卡片形式展示每条搜索结果；
- 分别设置标题、URL 和摘要的字号、颜色与间距；
- 为结果卡片增加轻微的悬停位移和阴影；
- 对过长 URL 使用自动换行，避免撑破页面；
- 增加无结果提示区域；
- 使用媒体查询适配窄屏和移动设备。

AI 还协助编写了主页前端参考代码，将原有简单表单调整为包含站点名称、副标题、搜索输入框和搜索按钮的完整首页，并建议使用 `url_for('query')` 生成表单提交地址、使用 `url_for('static', filename='style.css')` 加载样式文件，同时补充 `lang="zh-CN"` 和移动端 viewport 设置。

对于结果页，AI 协助编写了顶部搜索区域、结果数量提示、结果卡片循环和无结果状态的参考代码。结果链接建议在新标签页打开，并增加 `rel="noopener noreferrer"`；页面继续使用 Jinja 模板读取查询词、结果数量、标题、URL 和摘要。

本次前端协作中，AI 不仅提出界面设计建议，也提供了较完整的 CSS、主页 HTML 和结果页 HTML 参考实现。我负责理解、选择、调整并将需要的内容应用到自己的项目中，对最终页面代码和展示效果负责。

#### 11. 排查人名查询中的重复结果和短页面排名偏置

我发现搜索“毛佳昕”或“赵鑫”等人名时，排名靠前的结果中包含多个视频详情页、视频分类页和讲座页，其中若干 URL 实际展示相同或高度相似的内容。我认为这些结果挤占了 Top-K，降低了搜索结果的有效性和多样性，因此请 AI 排查原因。

AI 对照 `docID.jsonl`、本地 HTML、页面元数据和当前倒排索引进行了只读检查，并按现有 tf-idf 余弦计算方式重新计算了相关文档的得分。检查确认存在两类不同问题。

第一类是实际重复文档。例如，以下两个 URL 对应的本地 HTML 文件字节完全相同：

- `cate.html?cate_id=15&cate_name=...`；
- `cate.html?cate_id=15`。

此外，`cate.html?cate_id=15&page=1` 虽然原始 HTML 文件字节并不完全相同，但当前索引实际提取的标题、一级至三级标题和段落文本与前两个页面相同。因此，这三个页面在“毛佳昕”查询中的词频、加权词频和余弦得分完全一致，却分别占据三个结果位置。AI 还发现了教师列表页的 `/index.html` 变体和视频首页的 `/index.html` 变体等其他重复 URL。

第二类是当前余弦归一化对正文极短页面的排名偏置。视频详情页的页面标题是通用的“高瓴人工智能学院-视频中心”，正文段落为空，查询姓名只出现在一个标题元素中。由于页面中被索引的词项很少，文档向量长度较短，余弦归一化后反而得到很高的相似度。

AI 重新计算“毛佳昕”的排名后发现：视频详情页 `play.html?id=69` 得分约为 0.490，教师主页得分约为 0.364，讲座详情页得分约为 0.168；三个重复分类页得分均约为 0.153。对于“赵鑫”，排名第一和第三的结果也是正文很短的视频详情页，而教师主页排名第二。这说明问题不仅是网页内容重复，也包括短文档被余弦相似度过度奖励。

AI 建议按以下顺序改进：

1. 在元数据预处理阶段，根据用于检索的标题、各级标题和正文生成内容指纹；
2. 查询时先取比最终 Top-20 更大的候选集，再按内容指纹去除完全重复结果；
3. 对 URL 做谨慎的规范化，删除仅用于显示的参数，将 `page=1` 与没有 page 参数的首页视为同一分页页面，但保留 `id`、`cate_id` 等具有实际语义的参数；
4. 在基础 tf-idf 得分上增加查询词命中 HTML `<title>` 的加分，使“毛佳昕-教师系统”“赵鑫-教师系统”等明确主题页面优先于只在列表标题中出现姓名的页面；
5. 对分类页、列表页等聚合页面适当降权，避免列表中的一次姓名出现压过个人主页；
6. 如需进一步把视频页和对应讲座新闻聚合，可使用 SimHash、文本 shingles 或最大边际相关性重排，对高度相似结果进行聚类或多样性惩罚；
7. 保留原有自动评测所需的 URL 返回接口，在其内部增加候选集去重和重排，避免破坏接口形式。

AI 建议先实施“内容指纹去重 + 标题命中加分”，因为这两项改动较小、容易解释，也能直接解决当前最明显的问题；多样性重排可以作为后续扩展功能。此次仅进行了原因分析和方案讨论，AI 没有修改当前检索代码。

#### 12. 排查每条搜索结果连续显示两次的问题

我按照上一轮建议加入内容指纹、URL 规范化、候选集去重和标题命中优先级后，搜索“高瓴”时发现页面虽然显示 20 条结果，但每个 URL 都连续出现两次，而且多个视频页面显示相同的站点级摘要。我将实际页面输出反馈给 AI，请其继续定位。

AI 检查当前 `main.py` 后发现，`search()` 在同一次候选循环中执行了两次 `results.append(url)`。第一次追加发生在记录规范 URL 之后，第二次追加发生在记录内容指纹之后。因此，每个通过去重检查的 URL 都会被连续加入结果列表两次；当列表长度达到 20 时，实际只处理了 10 个不同候选。这与页面上“同一结果恰好连续出现两次”的现象完全一致。

AI 同时统计了 `docID.jsonl`，确认当前共有 15886 条记录和 15886 个不同 URL，不存在相同 URL 被保存两次的情况。因此，本次成对重复不是爬虫或倒排索引造成的，而是 Day 5 结果组装中的重复追加。

AI 建议只保留一次 `results.append(url)`，并在追加后记录内容指纹，再根据去重后的结果数量判断是否达到 (k)。当前 URL 规范化和内容指纹集合的总体思路可以保留。

对于不同视频页显示相同摘要的问题，AI 检查发现这些页面的 meta description 都是“新首页_中国人民大学高瓴人工智能学院”一类站点级通用文案。当前元数据生成程序只要发现 description 就优先使用它，因此不会回退到视频页面的具体标题。AI 建议将通用站点描述视为无效摘要；视频页正文段落为空时，可使用第一个 `h1` 至 `h3` 标题作为摘要或展示标题。这个问题与结果成对重复相互独立，需要分别修正并重新生成 `page_metadata.jsonl`。

AI 进一步指出，搜索“高瓴”时大量不同视频页排在前面还有第三个原因：当前代码使用布尔条件将“页面标题包含完整查询词”的全部候选整体移动到前面，而所有视频页的通用标题“高瓴人工智能学院-视频中心”都包含“高瓴”。因此标题优先规则会把整批视频页一起置顶。不同视频 `id` 的标题元素和内容指纹并不相同，精确内容去重不会删除它们；这属于同一路径类型结果过多造成的多样性问题，而不是完全重复文档。

AI 建议不要对通用站点标题使用强制置顶规则，可以先移除该二值排序，或先剥离站点名称和通用模板文字后再判断查询是否命中页面特有标题。为保证结果多样性，还可以按 hostname 和 URL path 建立结果族，例如将所有 `/addons/video/video/play.html?id=...` 归为同一族，在 Top-20 中最多保留两条；视频分类页族最多保留一条。搜索时仍从较大的候选集合中继续选择其他类型页面，以补足 20 条结果。

本轮中 AI 进行了代码审查、数据计数和原因说明，没有直接修改 `main.py` 或元数据生成代码。

#### 13. 实现查询词高亮显示

我希望像 Google 搜索结果一样，在结果标题和摘要中将与查询匹配的部分标红显示，因此请 AI 直接协助实现该功能。本轮中，AI 修改了 `day5/main.py`、`templates/res.html` 和 `static/style.css`。

AI 在 Flask 中注册了名为 `highlight` 的 Jinja 模板过滤器。过滤器将查询内容按空白拆分为一个或多个关键词，使用 `re.escape()` 处理关键词中的正则表达式特殊字符，并采用不区分英文字母大小写的匹配方式。为避免较短关键词抢先匹配较长关键词，过滤器先按长度从长到短排列去重后的查询词。

高亮实现没有直接对网页原始标题和摘要使用 `safe`。AI 使用 MarkupSafe 的 `escape()` 分别转义普通文本和匹配文本，仅由程序生成的 `<mark class="query-highlight">` 标签被保留为 HTML，从而避免本地网页标题或摘要中的 HTML 内容被浏览器当作可执行标签渲染。

结果页模板中的标题和摘要分别改为通过 `highlight` 过滤器渲染，URL 保持原样，不进行高亮。CSS 中新增 `.query-highlight` 样式，使用红色、透明背景和粗体显示命中部分。该功能支持中文完整查询，例如“毛佳昕”和“高瓴”，也支持以空格分隔的多个查询词。

本次高亮功能由 AI 根据我的明确要求直接写入项目文件。我负责检查最终实现、运行效果和提交内容。

#### 14. 在多样性过滤后尽量保证至少返回 20 条结果

我提出搜索页面应至少返回 20 条结果。AI 检查当前搜索流程后指出，候选集虽然扩大到了 200 条，但内容指纹去重和视频结果族数量限制可能使最终结果不足 20 条。直接复制结果或重新加入完全重复页面虽然能够增加数量，但会破坏此前去重的目的。

AI 将搜索过程改为两阶段选取。第一阶段从更大的候选集合中选择结果，继续执行规范 URL 去重、内容指纹去重，并限制视频详情页和视频分类页在结果中的数量，优先保证结果多样性。候选集合大小设置为 `max(500, k * 25)`，默认返回 20 条时会检查前 500 个候选。

如果第一阶段在严格执行视频结果族限制后仍不足 \(k\) 条，第二阶段会重新检查第一阶段暂缓的候选，放宽视频结果族数量限制以补足结果。第二阶段仍然执行规范 URL 和内容指纹去重，因此不会用相同 URL 或完全相同内容重复填充结果。

如果整个倒排索引对某个查询本来就不足 20 个不同的相关页面，程序只返回实际存在的不同结果，不伪造或复制页面。本轮修改由 AI 根据我的要求直接写入 `day5/main.py`，并同步更新本使用记录。

#### 15. 修复基础评测客户端无法导入 Day 4 检索模块的问题

我按照课程提供的方式进入 `students_evaluation/main` 并执行 `python client.py`，程序在导入评测接口时出现 `ModuleNotFoundError: No module named 'day4'`。我将异常信息提供给 AI，请其修复评测入口。

AI 检查 `client.py`、`students_evaluation/main/search_engine.py` 和评测 README 后确认：直接运行 `client.py` 时，Python 的模块搜索路径以 `students_evaluation/main` 为起点，位于 `hw/day4` 的包不在搜索路径中，因此 `from day4.Inverted_index import Inverted_index` 无法成功。

AI 还发现，评测接口原先使用 `Path(__file__).resolve().parents[1]` 计算项目根目录，但对该文件而言 `parents[1]` 是 `hw/students_evaluation`，不是 `hw`。即使模块导入问题得到解决，程序随后也会错误地在 `students_evaluation/inverted_index` 中查找索引和页面元数据。

AI 将评测接口的项目根目录改为 `Path(__file__).resolve().parents[2]`，并在导入 Day 4 模块前将该绝对路径加入 `sys.path`。这样无论当前终端位于 `students_evaluation/main` 还是其他目录，评测代码都能定位 `hw/day4`、`hw/inverted_index`、`hw/downloaded_html` 和 `hw/stopwords.txt`。

评测接口中还残留了从 Day 5 Web UI 复制的 Flask、模板渲染、请求对象和 MarkupSafe 导入，但这些内容不参与 `evaluate(query)`。AI 删除了这些未使用的前端依赖，使基础评测接口只保留检索所需内容。本轮由 AI 直接修改 `students_evaluation/main/search_engine.py`，并继续进行导入和路径验证。

AI 最初按项目先前约定在 `conda ML` 环境中验证，发现该环境缺少 Beautiful Soup、jieba、html5lib 和 Flask。AI 请求安装依赖，但我没有授权该环境修改，并随后说明评测应使用 `conda html`。AI 改用 `conda html` 后确认所需依赖均已安装；评测接口能够正确导入，计算出的 `PROJECT_ROOT` 为 `hw`，成功加载 15886 条页面元数据，并且 `evaluate("高瓴")` 返回了 20 条 URL。AI 只执行了本地导入和查询检查，没有连接评测服务器，也没有提交或打印服务器下发的正式评测查询。

#### 16. 将按天划分的作业重组为按功能划分的项目

随着抓取、索引、Web UI 和评测功能逐步连通，我认为继续使用 `day1` 至 `day5` 作为代码目录不利于理解最终系统，因此要求 AI 重新组织项目。AI 在移动前检查了 Git 状态、受版本控制文件、未提交修改、跨目录导入和本地运行数据，确认只重组 `hw` 作业项目，不改动外层课程讲义。

AI 将当前代码改为按职责组织：

- `ruc_search/` 保存 HTML 解析、网页抓取、倒排索引、页面元数据生成和统一搜索服务；
- `web/` 保存 Flask 入口、Jinja 模板和 CSS；
- `archive/` 保存早期爬虫、布尔索引和示例网页；
- `docs/ai_disclosures/` 保存各阶段 AI 使用记录；
- 项目根目录保存新的 `README.md`、`requirements.txt` 和 `LICENSE`。

已下载的 15886 个网页、倒排索引、页面元数据、停用词表和断点文件没有随代码移动。AI 认为这些文件数量较大且已经被 `.gitignore` 排除，保留原位置可以避免大规模移动数据带来的风险；核心代码统一通过项目根目录构造其绝对路径。

为避免 Web UI 和评测接口各自复制 URL 规范化、内容去重、视频结果族限制和两阶段补足逻辑，AI 新建 `ruc_search/service.py`，使用 `SearchService` 统一加载索引与页面元数据并提供 `search()`。Web 应用和 `students_evaluation/main/search_engine.py` 均改为调用该服务。

AI 同步修正了各模块导入、爬虫输出路径、索引构建时本地 HTML 路径、元数据命令和 Web 启动命令。新的 README 说明了功能目录、`conda html` 环境和常用模块命令；依赖集中记录在 `requirements.txt`。

重组后，AI 在 `conda html` 中完成了全部 Python 文件语法编译，并用“高瓴”“毛佳昕”和“赵鑫”验证统一搜索服务，三次查询均返回 20 个不同 URL。Flask 首页和结果页通过测试客户端检查，状态码均为 200，“高瓴”结果页包含 20 个结果卡片和查询词高亮。基础评测接口也能从 `students_evaluation/main` 目录导入同一服务，并返回 20 个不同 URL。本轮由 AI 直接执行文件迁移、代码重构、说明文档编写和验证。

#### 17. 合并各阶段 AI 使用记录

项目按功能重新组织后，我要求将原来分散在 Day 1 至 Day 5 目录中的 AI 使用记录合并为一份项目级文档。AI 按时间顺序保留五份记录的完整正文，统一调整 Markdown 标题层级，并在项目根目录生成 `AI_disclosure.md`。原 `docs/ai_disclosures/` 中的分散文件随后删除，README 中的目录说明同步更新。此后与本项目有关的新协作记录统一追加到根目录的总披露文件。

### 三、AI 建议的采纳与待处理情况

目前已采纳的建议包括：

- 从 Day 5 课件中确认 Web UI、前 20 个 URL 和扩展功能要求；
- 将前 20 个 URL 查询封装为独立函数；
- 将 Flask 服务启动放入主程序保护；
- 使用模块方式启动 Day 5 程序；
- 不在查询阶段实时访问最多 20 个外部网页；
- 从已经下载的本地 HTML 中预先提取标题和摘要；
- 使用 JSONL 保存文档元数据；
- 为标题和摘要设置多级提取与回退规则；
- 使用临时文件和原子替换保存元数据；
- 启动时一次性加载 URL 到元数据的映射；
- 使用基于 `__file__` 的项目路径，降低对当前工作目录的依赖；
- 将元数据放入已被 Git 忽略的中间结果目录。
- 为页面元数据增加基于检索文本的内容指纹；
- 查询时扩大候选集，并加入内容指纹和规范 URL 去重；
- 对分类页的显示参数和第一页 URL 进行规范化；
- 对完整查询词命中页面标题的候选结果优先排序。
- 在标题和摘要中安全地高亮与查询匹配的文字。
- 使用两阶段候选选择，在保持 URL 和内容去重的同时尽量补足至少 20 条结果。
- 修复基础评测接口的模块搜索路径和项目数据路径。
- 将项目从按天划分重组为核心检索、Web、历史代码和文档四类功能目录。
- 使用统一 `SearchService` 同时服务 Web UI 和自动评测。
- 新增项目级 README 和依赖清单，并验证新的模块启动方式。

仍待继续确认或处理的项目包括：

- 评估标题命中加分、列表页降权和短文档归一化调整；
- 根据需要实现近重复聚类或多样性重排；
- 根据需要继续优化结果页面；
- 根据实际页面名称和展示需求调整 AI 协助编写的 CSS、主页和结果页前端代码；
- 在实验报告中说明“本地标题和摘要预提取”扩展功能；
- 完成 Day 5 Git 提交、bundle 生成和验证。

### 四、使用说明与声明

AI 在本次任务中主要承担课件要求梳理、代码审查、运行错误分析、性能问题解释、元数据方案设计和修改建议的作用。AI 还协助编写了离线元数据处理参考代码、CSS 样式、主页前端和结果页前端的参考实现，并在获得明确要求后直接修改了部分 Day 5 文件和基础评测接口。

Day 5 最终保存到项目中的程序代码、页面模板、样式文件、元数据生成脚本和说明文件由我检查、选择和负责。我理解并决定是否采纳或调整 AI 提供的代码与建议，并对最终代码、运行结果和提交内容负责。本 `AI_disclosure.md` 由 AI 按照我的要求，根据本次真实协作过程持续整理和更新。
