# AI 使用披露

本项目使用 AI 辅助部分功能的设计与实现。各模块的具体分工如下。

## `crawler/`

爬虫的主体部分由我独立完成，包括链接发现、网页下载、断点保存以及 HTML 文件的本地存储等功能。

爬取时根据文档内容进行去重的部分完全由 AI 完成，包括内容指纹的生成、完全重复检测、近似内容判断以及重复 URL 的记录。AI 还协助补充了对畸形 URL 的异常处理，使单条无法规范化的链接不会中断整个爬取过程。

## `info/`

`info/` 中 docID、URL 与本地 HTML 文件路径之间的映射和管理由我完成。`info/page_info.py` 中使用 Resiliparse 解析本地 HTML，并提取网页标题、正文、摘要以及其他页面字段的工作由 AI 完成。

## `ruc_search/`

TF-IDF 检索和 Embedding 检索的独立实现均由我完成。我分别完成了 TF-IDF 倒排索引、词法相似度计算，以及网页分块、Embedding 生成和向量相似度检索。

AI 协助完成了 TF-IDF 与 Embedding 的融合，包括两类候选结果的合并、词法与语义信号的组合排序，以及融合后的检索结果调整。

AI 在独立实验分支中协助将字段加权 TF-IDF 扩展为可切换的字段加权 BM25，实现了 BM25 文档长度统计、旧索引的无损升级、五种检索模式的消融评测脚本与相关测试，并根据实际评测结果调整了汇报材料。

## 接口与工具使用说明

AI 还协助说明了项目中部分第三方库和接口的用法。例如，AI 讲解了如何使用 Resiliparse 解析 HTML 并提取可见文本，以及如何使用 SentenceTransformer 和 tokenizer 完成文本分块、Embedding 生成、向量归一化和相似度计算。这些协助主要用于理解相关 API 和排查调用问题，代码实现的分工仍以上述各模块说明为准。

## RAG

RAG 部分中的数据清洗主要由 AI 完成，包括正文空白字符归一化、重复句子和重复内容过滤，以及在构造大模型上下文前对多个检索结果进行整理和截断。

AI 还协助将 RAG 功能接入 Web 界面，包括复用已有检索服务、添加问答入口、展示生成答案与来源页面，以及处理 API Key 缺失和模型调用失败等异常情况。

AI 曾协助实现并测试多轮迭代检索控制器，以及针对接口超时的重试与证据摘录降级方案。实际评测显示这些方案增加了延迟并容易产生偏离问题的答案，因此最终版本按评测结果恢复为一次上下文构建和一次模型回答；多轮控制器及其降级逻辑未保留在最终实现中。

AI 进一步修复了 RAG 子查询规划中的实体粘连和跨年份召回不完整问题，包括为多个高信息实体分别生成检索词、逐年检索、交错合并各子查询结果，并在上下文中保留结果所命中的检索词。该修改采用通用规则，没有写入评测题答案。

AI 还完成了 Embedding 模型离线加载入口的统一：强制启用 Hugging Face 相关离线环境开关，确保 SentenceTransformer 只读取本地缓存，并为缓存缺失提供立即失败且可理解的错误信息。

## 汇报材料

AI 协助组织了项目汇报的叙事结构，并编写了 Beamer 幻灯片的框架、版式和待填充提示。具体项目数据、实验结果、案例与最终汇报内容由我补充和确认。

## 声明

我对 AI 辅助产生的内容进行了检查和测试，并对项目的最终代码、运行结果与提交内容负责。
